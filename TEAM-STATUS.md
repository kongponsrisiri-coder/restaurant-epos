# SiamEPOS Team Status Board

> Every agent reads this at the **start** of every session.
> Every agent updates this at the **end** of every session.
> This keeps the whole team on the same page.

---

## 🟢 Active Work

| Agent | Working On | Ticket | Started |
|-------|-----------|--------|---------|



---

## ✅ Recently Completed (last 14 days)

| Date | Agent | Completed | Ticket |
|------|-------|-----------|--------|
| 2026-05-26 | Krit | **SEPOS-BATCH-001 — Kitchen batch prep + expiry with chef tick-off (commit `0701615`).** Thai-kitchen inventory addition: chef defines batch recipes (curry pastes, stocks, sauces, marinated meats), system tracks per-batch expiry + locked cost, and menu recipes consume the batch like any other ingredient. **Schema (PG only — inventory is Pro plan, runs cloud-direct):** new `batch_recipes` (id, name, output_quantity, output_unit, shelf_life_days, total_cost, cost_per_unit, notes), `batch_recipe_lines` (FK + ingredient + qty + unit + line_cost), `batches` (made_on, expires_on, original_quantity, locked_cost_per_unit, status active/expired/discarded/used_up, discarded_qty/at/by, extended_count). `ingredients` gains `is_batch BOOLEAN + batch_recipe_id` so a batch row IS an ingredient — menu recipes pick it up automatically. **Backend:** CRUD `/api/batch-recipes` (POST auto-creates matching `ingredients` row; DELETE rejects 409 if any menu recipe line references it). `POST /api/batches/make` is the atomic make transaction — deducts every raw line, writes a `batch_prep` stock_movements row per line, computes total_cost from current ingredient prices (locks cost at make-time so paste cost stays stable when ingredient prices move next week), increments batch ingredient `current_stock` by output_quantity + snaps its `cost_per_unit`, inserts the batches row with `expires_on = CURRENT_DATE + shelf_life_days`. `POST /api/batches/:id/discard` — body `{quantity, reason}` default qty = original; subtracts from stock (clamped ≥0), writes 'waste' stock_movement with `cost_at_time = locked_cost_per_unit` so wastage report sees £ loss, status='discarded'. `POST /api/batches/:id/extend` — `expires_on +1 day`, status='active', `extended_count++`, capped at 3 extensions. `GET /api/batches` auto-flips `active→expired` on read so UI is current without a cron. **Frontend** — new "🥣 Batches" tab between Recipes and Stock. Two-pane: LEFT active batches with days-left countdown (≤1 day = amber border + "expires today/tomorrow"; expired = red border + Discard / "Still good (+1 day)" tick-off buttons); RIGHT batch recipes + "+ New recipe" + "🥣 Make" per row. MakeBatchModal previews exactly what'll happen before commit. DiscardModal asks "how much was left?" + reason + live wastage £. BatchRecipeModal filters ingredient picker to raw-only (no batch-from-batch — prevents cost loops). RecipesTab ingredient picker shows 🥣 prefix on batch ingredients. CSV export. **v1 limitation noted:** no per-batch FIFO at sale time. Total stock tracked at ingredient level; batches table is parallel tracking for expiry + discard management. v2 if a restaurant needs strict FIFO accounting. **Schema migrates on boot — Korakot to redeploy backend.** **Handoff to Nook:** create batch recipe (e.g. Red Curry Paste 2kg from 4 raw) → tap Make → verify raw stock deducted + batch stock +2kg + cost locked → add batch ingredient to a menu recipe → sell a dish → verify ingredient stock decremented → wait for expiry (or temporarily set shelf_life=0) → verify status auto-flipped → tick Discard with partial qty → verify wastage report shows £ loss → tick "Still good +1 day" on another expired → verify expires_on bumped + status back to active. | SEPOS-BATCH-001 |
| 2026-05-25 | Sam | **Online booking correctness batch — Stripe-less graceful degradation + past-date / past-slot blocking + date-first widget reorder + no-cache widget headers (commits `5193620`, `8cde26f`, `1974d71`, `cbbbe67`, `1e6ccf4`).** Five-commit batch fixing real correctness gaps Korakot surfaced while testing the live spa widget. **(1) `5193620` Stripe-less graceful degradation.** Korakot reported online bookings bouncing with "payment_intent_id required (deposit due)" even though the widget UI never showed a payment step. Root cause: client/server disagreement — widget correctly checks `/stripe-config` and skips Stripe when `configured: false`, but server's `/book` was strict (deposit_model != 'none' AND amount > 0 = must have payment_intent_id regardless). Fix: gate the deposit-enforcement block on `stripeClient()` being non-null. If Stripe isn't configured we silently skip the deposit check, log a warning, and let the booking through with `deposit_amount=0`. Operator can still capture deposits via SPA-DEPOSIT-MANUAL once the booking lands. **(2) `8cde26f` past-date blocking + date-first reorder.** Two bugs in one ship. **Past dates:** `POST /api/widget/book` rejects `starts_at < now` with 400 "cannot book in the past"; `POST /api/appointments` (admin) rejects too, with `?allow_past=1` opt-out for recording a historical booking. Admin `NewAppointmentModal` date input gains `min=today` on CREATE (omitted on EDIT so a 14:00 booking can still be shuffled to 13:30 within today). **Date-first reorder (widget):** still 6 steps but combined Step 2 = Date+Therapist. Date picker on top with `min=today`, therapist list below filtered to whoever's on shift via new `GET /api/widget/therapists?date=YYYY-MM-DD` (reuses `getTherapistWorkingWindow` from availability service). Step 3 becomes Time-only with a "Saturday 25 May · with Anchara" header. If date changes and the picked therapist is off, selection auto-resets to "Any available". **(3) `1974d71` no-cache headers on widget.js.** After ship #2, Korakot reported the new widget wasn't visible — curl confirmed Railway WAS serving the new file but his browser cached the old one. Added `Cache-Control: no-cache, must-revalidate` to `/widget.js` + `/booking-widget.js`. Express auto-sets an ETag so unchanged widgets return 304 (~0 bytes, ~50ms) — only changed widgets ship the full ~30KB. After this commit, all future redeploys propagate on next page load instead of waiting hours for cache TTL. **(4) `cbbbe67` belt-and-braces past-date guard for mobile pickers.** HTML5 `<input type="date">` min attribute isn't strictly enforced on every browser (Safari iOS notorious for scrolling past min). Three new layers on top of the existing `min=today`: (a) `onChange` handler that snaps `state.date` back to today + shows clear error if user picks a past date; (b) amber warning banner under the input when `state.date < today`; (c) Continue button hard-disabled when past + re-validates on click. Server-side rejection from `8cde26f` already in place as final safety net. **(5) `1e6ccf4` past time-slot filter on widget /availability.** Korakot's actual complaint after #4: not past DATES but past TIME slots for today. At 14:00 the widget was still showing 10:00 slots. `computeAvailability` generates slots from opening_time onward without comparing to now. Fix at the widget endpoint only (admin keeps full slot list for retroactive walk-in entries): filter out any slot starting before `now + 30 min`. The 30-min lead-time buffer prevents "book a slot starting in 2 minutes" which would leave no room prep time. **Test plan for Nook:** open widget → step 2 shows date picker on top + therapist list below filtered by rota · pick yesterday → blocked, amber banner appears · pick today + therapist who's off today → therapist tile doesn't appear · pick today + Anchara at 14:00 → step 3 shows only slots ≥ 14:30 · curl `POST /api/widget/book` with past `starts_at` → 400 "cannot book in the past" · curl with no `STRIPE_SECRET_KEY` env → booking goes through with `deposit_amount=0`. | SPA-WIDGET-FIXES |
| 2026-05-25 | Sam | **SPA-DEPOSIT-{BADGE,MANUAL,DAILY} — deposit visibility across timeline + modal + summary card (commit `f207917`).** Korakot's ask: the team should be able to see at a glance which bookings have a deposit on file, and the receptionist needs a way to record a deposit taken by cash or card terminal (not just Stripe). Three small features built together because they share the same `appointments.deposit_*` columns. **(1) SPA-DEPOSIT-BADGE — 💳 corner badge on timeline blocks.** Active bookings with `deposit_amount > 0` show a green corner badge (`💳 £25`) in the top-right of the appointment block on TimelineView. Pending Stripe links show amber. Title attribute carries exact amount + method on hover. Hides automatically once `status='completed'` so the bottom-right payment-method badge isn't crowded. Legend row gains "💳 £ — Deposit on file". Receptionists see what's already collected without opening anything. **(2) SPA-DEPOSIT-MANUAL — Take deposit at till (cash / card terminal).** Three new schema columns: `deposit_method` (cash / card), `deposit_taken_at` (timestamp), `deposit_taken_by` (therapist FK). New backend endpoints — `POST /api/appointments/:id/deposit-manual { amount, method }` refuses if a Stripe deposit already exists, refuses if the bill is closed, otherwise UPSERTs deposit_amount + method + sets `payment_status='deposit_paid'`. `DELETE /:id/deposit-manual` clears (operator mistake). Both emit `appointment_updated` socket. UI: new `ManualDepositBlock` in `NewAppointmentModal` — amber "💵 Take deposit at till" panel with £ amount input + quick-fill buttons (policy default £25 + full treatment price) + two payment buttons (💵 Cash / 💳 Card). Existing manual deposit renders a green "💵 £X CASH/CARD deposit taken HH:MM" line with a Clear button. Block hides if a Stripe deposit exists (operator can't overwrite online) or if the bill is closed. Confirm dialog before recording. The existing bill auto-credit logic in `bills.js` already reads `appointment.deposit_amount`, so manual deposits flow through transparently — operator collects only the balance at checkout. **(3) SPA-DEPOSIT-DAILY — Daily summary card on AppointmentScreen.** Cream/gold banner above the timeline showing: `💳 Today · 4/12 with deposit · £100.00 collected · 🌐 £50 online · 💵 £25 cash · 💳 £25 card · ⏳ 1 pending link`. Computed client-side from the appointments list already in state — no extra API call, auto-refreshes on socket events. Mobile collapses to count + total only. Backend also exposes `GET /api/appointments/deposit-summary?date=…` for any future external dashboards. **Schema migrates on boot** — Korakot to redeploy backend. **Test plan for Nook:** (a) create booking via admin → ManualDepositBlock visible → record £25 cash → badge appears on timeline · (b) open the booking → "💵 £25 CASH deposit taken" line → Clear → badge disappears · (c) generate Stripe deposit link → ManualDepositBlock hides (correct — can't overwrite online) · (d) close bill at checkout → bill total subtracts deposit · (e) refresh AppointmentScreen → daily card shows correct breakdown. | SPA-DEPOSIT-BADGE, SPA-DEPOSIT-MANUAL, SPA-DEPOSIT-DAILY |
| 2026-05-25 | Sam | **SPA-TREATMENT-VISIBILITY + duration-first booking + Treatwell one-tap + payment confirm dialog.** Four small Korakot-requested polish ships. **(1) SPA-TREATMENT-VISIBILITY (commit `bbd4805` — part 1).** Spa owners don't want every menu item on the public widget — VIP packages, long rituals, treatments under trial, etc. should be in-store/phone only. New `treatments.online_bookable BOOLEAN NOT NULL DEFAULT TRUE` column (defaults TRUE so nothing changes for live spas). `GET /api/widget/treatments` filters `active = TRUE AND online_bookable = TRUE`; admin `/treatments` still returns everything plus the flag. POST + PUT accept `online_bookable` (COALESCE with TRUE so omitted body fields don't accidentally hide a treatment). TreatmentMenuSection Edit modal gains a green/amber checkbox panel "🌐 Show on online booking widget"; list rows show a yellow "🚫 in-store only" pill on the treatment name when the flag is off. **(2) Duration-first booking (commit `bbd4805` — part 2).** When a spa has many treatments at similar lengths, picking the right one is faster if you narrow by duration first. NewAppointmentModal gains a "Duration" row of pill buttons above the treatment select — `[All] [30 min] [45 min] [60 min] [90 min] …` built dynamically from the unique durations in the treatment list. Default `'all'`. Picking a duration narrows the treatment dropdown; picking a treatment directly auto-syncs the filter to that treatment's duration so the UI stays coherent. Empty-state helper appears when a filter has no matches. **(3) Treatwell button — one-tap for full prepay (commit `41377c8`).** Korakot reported: "for Treatwell prepay, customer already paid full amount but I can't put £0 on payment". The TreatwellPaymentPanel forced an amount input; for full-prepay bookings that's pointless (the answer is always the bill total) and the mental model "I collected £0" didn't fit the panel's "what Treatwell paid" prompt. Fix: branch on `appt.treatwell_payment_type` — `'partial'` → open the panel, anything else → one tap closes the bill as `method='treatwell'` (bill total stays at full price = what Treatwell settles minus commission; till takes £0). Tooltip adapts. **(4) Payment confirm dialog (commit `3a3cb23`).** New `payConfirm(method, label)` wrapper in CheckoutScreen — Cash / Card / Treatwell-full-prepay now `confirm()` before closing. Messages: "Confirm: take £30 by CASH and close this bill?", "Confirm: take £30 by CARD and close this bill?", "Confirm: close this bill as paid by TREATWELL? (Till takes £0; Treatwell settles to your account)". Split modal, Voucher panel, Treatwell-partial panel already have their own inline confirmation — skipped to avoid double confirm. Voucher legacy no-code button already had its own confirm() (existed before this change). | SPA-TREATMENT-VISIBILITY, SPA-DURATION-FILTER, SPA-TREATWELL-1TAP, SPA-PAY-CONFIRM |
| 2026-05-25 | Sam | **SPA-OWNER-NOTIFY — browser pop-up + email to owner on every new booking (commit `78a6690`).** Korakot's ask: spa owner wants to know when a new booking arrives. Two channels, no new deps. **(1) Browser desktop notification.** New 🔔 button in the TopNav right-side. Default state: outline + "🔔 Enable" label. Click → `Notification.requestPermission()`. On grant: gold-bordered + 🔔-only label + a confirmation pop-up. On deny: 🔕 greyed with tooltip on how to re-enable via browser settings. TopNav subscribes to the socket `new_appointment` event (every booking site already emits it) and fires `new Notification('🔔 New booking', { body, tag })` — works even when the tab is in the background; clicking the pop-up focuses the spa tab. **(2) Email to spa_email.** New `sendOwnerNewBookingEmail()` in `src/services/emailService.js`. Reads recipient from `settings.spa_email` with `SPA_EMAIL` env fallback; silent skip if neither. Branded HTML mirrors customer confirmation: navy header / gold accent / booking summary table (treatment, time, therapist, source label — Online / Treatwell / Phone / Walk-in / Staff — client phone/email, deposit/balance, reference #). Subject: `🔔 New booking · {Client} · {when}`. Wired into 3 call sites, all fire-and-forget: `widget.js POST /book` (source=online), `treatwell.js POST /webhook` (source=treatwell), `appointments.js POST /` (source from request body). Treatwell + admin paths do a small follow-up SELECT to attach therapist + client + treatment names since INSERT only returns raw columns. Errors are caught + console.error'd — never block the booking response. 5 files, +193/−4. **Required on Railway for live emails:** `BREVO_API_KEY`. Without it `sendBrevoEmail` returns `{skipped:true}` and the booking still completes cleanly. The "off switch" mechanism (clear `settings.spa_email`) is documented in the email footer; a per-toggle setting is a separate ticket if a spa specifically asks. | SPA-OWNER-NOTIFY |
| 2026-05-25 | Krit | **SEPOS-VOUCHER-001 follow-up — at-till sell flow in admin (commit `0b9a70a`).** Korakot tested the v1 shipped earlier and surfaced two gaps: (1) the public widget wasn't showing on siamepos.net, (2) admin → 🎁 Vouchers was read-only — operator couldn't sell a voucher to a walk-in customer paying cash/card. Gap #2 was a real scope miss: the original v1 wired online purchase + EPOS redemption, but the over-the-counter sell path was missing. **Backend (`src/server.js`):** new `POST /api/vouchers/sell` — admin endpoint, no Stripe PI verify, body `{amount, payment_method:'cash'\|'card', recipient_name?, recipient_email?, sender_name?, message?}`. Creates voucher with `payment_method='cash'` or `'card'` (distinguishable from Stripe + mock sales for Z-Report accounting later). Same `validateAmount` £10–£500 helper as widget. **Admin (`VouchersSection.jsx`):** new gold "+ Sell voucher" button next to Export CSV. `SellVoucherModal` — £25/50/100/150 presets + custom field, recipient name + email (email **optional** so operator can hand a printed gift card), sender, message, Cash/Card payment toggle. `SoldSuccessModal` — gold gradient code reveal so operator can read the code aloud or write it on the card if no email, plus confirms email status. List auto-refreshes after sale. **Gap #1 diagnosis:** siamepos.net is the drag-and-drop deployed Netlify site (not git-connected — same situation as the 2026-05-21 reservation bug, memory `feedback_diff_live_vs_repo` already flagged this). Curled live HTML, confirmed it has the takeaway widget but not the voucher widget script. Korakot needs to drag-and-drop `client/MockUp Website/` to Netlify (5 min) — OR finally link the site to GitHub via Netlify Site settings → Build & deploy → Link site to Git, base directory `client/MockUp Website`. Recommended the git-link permanently to stop this biting us each release. | SEPOS-VOUCHER-001 |
| 2026-05-25 | Krit | **SEPOS-VOUCHER-001 — Gift voucher selling (monetary, v1).** Full end-to-end loop shipped: public widget purchase → Stripe (or mock-pay) → branded Brevo gift email → admin matrix → EPOS redemption at checkout → Z-Report block. **Policy:** £10 min · £500 max · 24-month expiry · no refunds (Korakot's call). **Schema:** new `vouchers` + `voucher_redemptions` tables on PG + SQLite, both auto-migrate on boot (per-restaurant via `restaurant_id`). **Backend (`src/services/voucherService.js` + endpoints in `src/server.js`):** `GET /api/widget/voucher/config` (min/max/expiry/Stripe-publishable), `POST /api/widget/voucher/purchase` (creates Stripe PI or mock PI), `POST /api/widget/voucher/confirm` (server-side PI verify → INSERT voucher → fire gift email — voucher never born until payment verifies, so abandoned purchases leave no phantoms), `GET /api/widget/voucher/:code` (public balance lookup with auto-expire-on-read), `POST /api/vouchers/:code/redeem` (atomic FOR UPDATE balance decrement + redemption row — two-terminal double-spend safe), admin list/detail/void/resend-email. Codes `GIFT-XXXXXXXX` (8 chars, no ambiguous I/O/0/1). **Public widget (`public/voucher-widget.js`):** 4-step modal (amount/personalise/pay/success). Stripe Elements when `STRIPE_PUBLISHABLE_KEY` is set, one-click mock-pay otherwise — same SPA-PAY-001 fallback pattern Sam used for spa deposits, so the demo works on Baan Siam without Stripe creds. Auto-mounts on `<div id="siamepos-voucher">` OR `[data-siamepos-voucher]` buttons OR `SiamEPOSVoucher.open()`. Branded navy/gold. **EPOS (`BillScreen.jsx`):** 🎁 Voucher button alongside Cash/Card/Other. New `stage='voucher'` modal — code input → live `GET /api/widget/voucher/:code` balance preview → green "covers full bill" or amber "partial, customer pays £X remainder" depending on balance. Full redemption closes bill as `method='voucher'`. Partial redeems the available balance and applies as discount with reason `Voucher GIFT-XXXX −£X`, bill stays open for Cash/Card. **Admin (`AdminScreen.jsx` + new `VouchersSection.jsx`):** 🎁 Vouchers tab between Customers and Campaigns. Search, status filter, 5 stat cards (sold total / 30-day / active count / outstanding balance / redeemed off-till), detail modal with redemption history + Resend Email + Void. CSV export. **Z-Report (preview endpoint + `ZReportSection.jsx`):** new `vouchers_sold` + `vouchers_redeemed` blocks. Cream "🎁 Gift vouchers — settled to Stripe, not the till" card so owner doesn't double-count Stripe revenue against till cash. **Mockup site (`client/MockUp Website/index.html`):** added "🎁 Gift Voucher" CTA next to Order Takeaway in the hero; voucher widget script tag added. **Brevo gift email:** branded navy/gold layout, voucher code in large dashed gold-bordered box, sender's personal message in cream block, expiry date footer. Sent on `/confirm` (no delivery-date scheduler yet — recipient gets it immediately but can use any time before expiry). **Required Railway envs for real Stripe:** `STRIPE_SECRET_KEY` + `STRIPE_PUBLISHABLE_KEY`. Without them, mock-pay fallback runs (voucher tagged `payment_method='mock'`, excluded from Z-Report's Stripe revenue line). **Schema migrates on boot — Korakot to redeploy backend.** PWA cache bumped v4→v5 so iPads flush. Commits coming next push: backend + widget + EPOS + admin + Z-Report bundle. **Handoff to Nook:** QA full purchase→redeem flow (full voucher + partial), void + resend email, Z-Report block math. Test card `4242 4242 4242 4242` once Stripe envs are on. | SEPOS-VOUCHER-001 |
| 2026-05-25 | Krit | **Allergen chips on menu buttons now confirmed-only (commit `00825a0`).** Korakot's call: the AI scanner is a suggestion, the Allergen Matrix is the source of truth, and the red chips on the order-screen menu buttons should only appear once an allergen has been confirmed in admin. One-line behaviour change in `OrderScreen.jsx` — `allergensByItemId` previously fell back to `menu_items.allergens` (raw AI output) when there was no `dish_allergens` entry, so unreviewed AI guesses showed up to waiters as gospel red chips. Now reads `dish_allergens` only. The admin matrix's existing infrastructure handles the rest (already shipped — no changes needed there): AI-scanned dishes appear in the matrix tagged `🤖 AI scan` with blue ticks; the big blue "🤖 Confirm AI Allergens (N)" header button bulk-promotes them; per-cell tap promotes individually. **After deploy, each restaurant with existing AI-scanned dishes (Baan Siam, main siamepos) needs to open admin → 🌿 Allergen Menu and click "🤖 Confirm AI Allergens" once** to lock in their existing suggestions — otherwise chips will temporarily disappear on the order screen until reviewed. (This is exactly the safety behaviour we want for new scans, but feels regressive for already-trusted data.) Rationale: Natasha's Law (2021) makes a wrong allergen chip a real liability, so the matrix is the only legitimate source for waiters. | ALLERGEN-CONFIRMED-ONLY |
| 2026-05-25 | Krit | **TableMapScreen blank-page bug — actual root cause found + fixed (commit `b395dd9`).** Same `.toFixed`-on-PG-string class as yesterday's TradingSection bug, but in `TableMapScreen.jsx` — when a user tapped an online/takeaway order, the tap-action popup tried to render `£{(tableActionPopup.order.total || 0).toFixed(2)}`. Because pg returns NUMERIC as string, `"24.10".toFixed(2)` raised TypeError, which propagated up and unmounted **the entire TableMapScreen** (no error boundary above it) → whole page blanked. **This was the real cause of Korakot's "click online order → bill blank" symptom** — the bill modal never even mounted because the popup that opens it crashed first. The previous backend 404 re-fix (`bc3f3cc`) was treating a different (also real) issue but didn't address the visible symptom. Verified the API path was healthy via curl: live Baan Siam order #4 returns full `{order, settings}` shape with `total:"14.70"` (string). Both unsafe sites in TableMapScreen wrapped in `Number()`. Three more latent OrderScreen sites (`item.price.toFixed`, `modifierPopup.item.price.toFixed`, `opt.extra_price.toFixed`) picked up by a concurrent session as commit `b4c774f`. **Methodology lesson:** when chasing a "blank UI" symptom, always trace what renders FIRST in the chain — the symptom showed at the bill modal but the crash was upstream in the popup that opens it. Should have curl'd the API + inspected the live bundle before assuming the bill component was at fault. | TABLEMAP-BLANK-FIX |
| 2026-05-25 | Krit | **Admin .toFixed-on-PG-string audit + bill 404 re-fix (commits `e5d724f`, `bc3f3cc`).** Yesterday's TradingSection fix (`4297dad`) revealed a class of latent bug: PostgreSQL `SUM/AVG NUMERIC` returns as **string** via the pg driver, and any `.toFixed()` called directly on it raises a TypeError that unmounts the admin React tree. (This is what actually caused the Baan Siam "flash then blank" symptom; the bill-modal fix I reverted last week was innocent.) Audited every admin section + inventory tab. **Patched** (Number() wrap at every API-money render site): ReportsSection (order.total, total_dine_in/takeaway/sales), BillsSection (b.total reduces — were string-concatenating! — plus bill.total + bill.paid_amount), ZReportSection (~10 sites: total_sales/cash/card/dine_in/takeaway/discounts/avg_per_cover + p.value in payment-methods row + r.total_sales in history list), CostSalesTab (totalRevenue coerced at source — all downstream arithmetic was already number-safe), MenuSection (dish.price on AI scanner result). Verified-safe sites left alone: locally-computed reduces already using Number() (totalSales/totalCash/totalCard), arithmetic results (avgPerHead/avgPerCover/expectedCash/billTotal/subtotal/serviceCharge/vatTotal), parseFloat outputs (actualNum/floatNum), per-staff seconds (totalHours). BillPeek already had a safe `money()` helper. **Bill fix re-applied backend-only (`bc3f3cc`):** `GET /api/orders/:id/bill` now returns `404 {error:'Order not found'}` when the order doesn't exist (was previously spreading `undefined` into `{order:{items:[]}}` which is truthy and rendered blank). No frontend changes this time — minimal diff, can't possibly touch the admin tree. Frontend's existing `.catch` on `getBill` will surface the 404 in the err state. **Lesson for next time:** when PG returns NUMERIC fields, treat them as strings on arrival. Either coerce at the API layer or wrap every `.toFixed` site in `Number()`. The latent bugs sit dormant until a tenant's first SUM is non-zero. | ADMIN-TOFIXED-AUDIT, BILL-404 |
| 2026-05-25 | Sandy (Cowork) | **SPA-003 Treatwell — 4 bugs fixed + E2E test suite 31/31 ✅.** Korakot reported bad feedback on Treatwell booking and checkout. Deep investigation found 4 bugs: **(BUG-TW-001 CRITICAL)** `treatwell_payment_type` defaulted to `'partial'` for ALL standard Treatwell marketplace bookings because the final `else` branch in `treatwell.js` set partial when the payload had no `payment_type` field — but Treatwell's standard marketplace booking never sends that field. Fix: final else now defaults to `'full'`. **(BUG-TW-003 HIGH)** Double-paying an already-paid bill returned HTTP 200 silently (the UPDATE WHERE payment_status != 'paid' returned 0 rows, bill row was null, but no check was done). Fix: added `AND payment_status != 'paid'` to the UPDATE, then distinguished 404 vs 409 with a second SELECT. **(BUG-TW-004 MEDIUM)** Deleting a bill left `appointments.payment_status = 'fully_paid'` — deposit looked consumed even though the bill was gone. Fix: DELETE /api/bills/:id now rolls `fully_paid → deposit_paid` in the UPDATE appointments. **(BUG-SPA-PAY-002 CRITICAL)** Split payment failed for any appointment with an online deposit — the split sum validation compared against `bills.total` (full amount) but the frontend sends the balance (total − deposit). Fix: validation now joins `appointments.deposit_amount` and validates against the balance. **(BUG-TW-002 UX)** Treatwell checkout banner re-appeared after payment was recorded. Fix: CheckoutScreen now shows a green "✓ Treatwell payment recorded" strip when `bill.payment_method === 'treatwell'` or `discount_reason` contains "Treatwell paid". **server.js** refactored: boot IIFE guarded with `require.main === module` + `module.exports = app` so test runner can require the app without double-booting. **Test suite:** `test-treatwell.js` (11 blocks, 31 checks) + `test-treatwell-runner.js` (pg-mem harness: registers TRIM/NULLIF/LENGTH/NOW, rewrites LATERAL→stripped, NOT IN→inequalities, bcrypt PINs). **Commit:** `6146991`. **Push needed** (sandbox has no GitHub creds). | SPA-003, BUG-TW-001/002/003/004, BUG-SPA-PAY-002 |
| 2026-05-25 | Sandy (Cowork) | **BUG-SPA-PAY-002 + BUG-SPA-DISCOUNT-001 — 4 payment bugs fixed.** Korakot reported bad feedback on spa checkout payments. Root-cause investigation found 4 bugs: **(1) BUG-SPA-PAY-002 (CRITICAL)**: Split payment failed with HTTP 400 "does not match bill total" for ANY appointment that had a Stripe online deposit. The first split validation in `POST /bills/:id/pay` queried `bills.total` but not `deposit_amount`, then compared the frontend's split sum (which sums to `balance = total − deposit`) against the full bill total — always a mismatch. Fix: joined `appointments` to get `deposit_amount` in the split validation query; introduced `expectedSum = billTotal − depositAmount`; removed the now-dead second validation block that was supposed to handle this but was never reachable. 2 edits to `spa-epos/src/routes/bills.js`. **(2) BUG-SPA-DISCOUNT-001 (HIGH)**: Discount % preset buttons called `saveDiscount(amount, "${p}% off")` which SET the discount absolutely — silently wiping any Treatwell partial or voucher partial payment previously recorded as a discount. Fix: introduced `applyPercentDiscount()` guard in `CheckoutScreen.jsx` that checks if `bill.discount_reason` contains "Treatwell paid" or "Voucher " and shows a `confirm()` warning before overwriting. **(3) BUG-SPA-DISCOUNT-002 (MEDIUM)**: `discountReason` local state started as `''` and was never initialised from `bill.discount_reason` — so changing the custom amount input after page load wiped the saved reason. Fix: initialise `discountReason` + `discountDraft` from the bill in the `load()` callback after `setBill`. **(4) BUG-SPA-DISCOUNT-003 (MEDIUM)**: Custom discount £ input fired `saveDiscount()` on every keystroke via `onChange` — two API calls when typing "15" (saves £1 then £15). Fix: replaced with `discountDraft` local state + `onBlur` that only calls `saveDiscount` when the value actually changed. All 4 fixes verified with `node --check` (bills.js) and inline assertion script (CheckoutScreen). | BUG-SPA-PAY-002, BUG-SPA-DISCOUNT-001 |
| 2026-05-24 | Sam | **SPA-MENU-V2 — Treatment menu reorganised + hard-delete (commit `67b5b33`).** Korakot asked for a Delete button on treatments and a more organised menu. **Backend (`src/routes/treatments.js`):** `GET /treatments` learns `?include_inactive=1` (admin view, returns hidden too) and `?with_booking_count=1` (LATERAL JOIN exposes `booking_count + last_booked_at` per row — drives the front-end "can hard-delete?" gate). `DELETE /treatments/:id?hard=1` is the new permanent-delete path: counts referencing appointments first; if any exist, returns 409 with the count and "Hide it instead" message; otherwise hard DELETEs. Default `DELETE` stays soft (active=false) so existing call sites are unchanged. New `PUT /treatments/categories/:id` rename + `DELETE /treatments/categories/:id` (wrapped in a transaction that moves any treatments using it to category_id=NULL "Uncategorised"). **Frontend (`TreatmentMenuSection.jsx` — full rewrite, 274 → 341 lines):** search bar (name/description/category, case-insensitive); "Show hidden (N)" checkbox; clickable category pills → modal with rename + 🗑 Delete + booking count; treatments now grouped by category into collapsible sections (▼/▶ chevron, count badge, "+ Add to {category}" shortcut); per-row Bookings column with last-booked tooltip; three actions per treatment: **Edit · Hide/Restore · 🗑 Delete** (🗑 disabled with tooltip when bookings exist; **Restore** appears in place of Hide for already-archived treatments). Hidden rows render at 60% opacity with a "hidden" pill. Uncategorised treatments collect under a final "Uncategorised" group. E2E verified: hard-delete with bookings → 409 "Cannot delete — 15 appointments reference this treatment"; hard-delete clean treatment → 200 deleted; category rename + delete both work. | SPA-MENU-V2 |
| 2026-05-24 | Sam | **Spa identity on every report + drag-and-drop swap/move on timeline.** Late batch. **(1) Spa-name header on Trading + Reports (commit `4f0cd54`).** Z-Report already carried it; Korakot wanted the same on Trading and Reports. New shared `loadIdentity()` helper in `src/routes/reports.js` reads `settings.spa_name/spa_email/spa_address/spa_phone` with env-var (`SPA_NAME`/`SPA_EMAIL`/`SPA_ADDRESS`) fallback and ultimate fallback to "SiamEPOS Spa". Both endpoints return an `identity` block; TradingSection + ReportsSection render the centred Georgia heading + muted address/phone/email subline above their content. Reports CSV export prepends the identity in the first 3 rows. **(2) SPA-SWAP — backend swap endpoint (commit `4044d5c`).** New `POST /api/appointments/swap` body `{id_a, id_b}` — atomic exchange of therapist + room between two bookings. Same-time, same-day; both bookings keep their starts_at/treatment. Validation: each new therapist must be on shift at the OTHER booking's time AND have no other clash. Single transaction with `FOR UPDATE` lock. Emits two `appointment_updated` socket events. **(3) SPA-SWAP-DND — drag-and-drop on timeline (commit `1508fc7`).** Replaces the modal Pick-dropdown approach (removed `SwapBlock` from `NewAppointmentModal`). Now any active block on the timeline (booked/in_progress, desktop only) is draggable; dropping onto another active block fires the swap. Visual: source 45% opacity, target gets a 3px dashed gold border, grab cursor. Completed/cancelled/no_show blocks aren't draggable and not valid drop targets. Single gesture, single endpoint, single mental model. **(4) SPA-MOVE-DND — drop onto empty area (commit `9da9be1`).** Korakot reported dragging to another therapist just snapped back. Empty-area drops now MOVE the booking instead. Column container gets `onDragOver`/`onDrop` in addition to the existing slot-click handler. Drop computes new time from `dropY / HOUR_H + DAY_START`, rounds to **15-min snap** (matches the booking widget's slot grid), clamps to DAY_START..DAY_END. Parent calls `PUT /api/appointments/:id` with new `therapist_id + starts_at`; server enforces rota + conflict (existing). Hovered destination column tints cream (#fdf6ec) while dragging. Block-onto-block drop wins via `e.stopPropagation()` so swap still takes precedence on collision. | SPA-SWAP, SPA-SWAP-DND, SPA-MOVE-DND, REPORTS-IDENTITY |
| 2026-05-24 | Sam | **Timeline legend polish + Treatwell partial payment + Online colour swap.** Three small late ships after the SPA-CHECKOUT-V3 batch. **(0) Online → brown.** Walk-in (indigo) and Online (blue) were too similar — both in the blue family. Online recoloured to saddle brown (#8b4513 / #f5e6d3) so the booking-source row has zero ambiguous neighbours. **(1) Legend rebuild + Phone colour (commit `575f986`).** Korakot's feedback: Phone (teal) and Treatwell-prepaid (green) were too visually similar; the legend footer was cluttered with Cancelled/No-show which are self-evident from context; he wanted the checkout payment-method colours back as a reference. Fix: Phone → **purple** (#a855f7 / #f3e8ff) — couldn't be more different from green. Voucher payment also nudged green → emerald (#10b981) so it's distinct from Treatwell-prepaid's forest green. Legend now has two grouped rows: **SOURCE** (Phone / Walk-in / Online / Treatwell prepaid / Treatwell deposit) and **PAID BY** (Cash / Card / Voucher / Treatwell / Split). Cancelled + No-show removed. `apptStyle()` now flips the appointment block to the payment-method colour once `status='completed'` AND a `payment_method` is recorded — so a finished booking visibly shows how it was settled, while booked/in-progress shows the source colour. **(2) Treatwell partial deposit panel (commit `fcaa24b`).** The 🌐 Treatwell button used to always close the whole bill. Treatwell also has partial bookings (customer paid a deposit only) where the spa needs to record what Treatwell paid AND collect the rest at the till. New `TreatwellPaymentPanel` mirrors the legacy-voucher + monetary-voucher pattern: operator types £X (defaults to full bill total, "Full £X" one-tap button); amount=total → close as method='treatwell' (existing behaviour); amount<total → applied as discount with reason "Treatwell paid −£X", bill stays open for Cash/Card/Split. Hint text adapts to `treatwell_payment_type='partial'` flag set by the webhook ("Enter what Treatwell paid; the balance is due at the till"). All four partial-payment patterns (voucher / legacy-voucher / Treatwell / split) now share the same discount-line composition pattern so a single bill can stack multiple partial payments cleanly. 4 files net, +136/−16. | SPA-TREATWELL-PARTIAL, SPA-LEGEND-V2 |
| 2026-05-24 | Sam | **SPA-CHECKOUT-V3 — discount + legacy/partial vouchers + source-coloured timeline + booking-source dropdown + treatwell sub-types.** Big follow-up batch after the morning's SPA-TURN-ORDER. **(1) TurnOrder fix (commit `d54f926`).** Korakot reported the timeline column names didn't reorder after saving a turn order. Root cause: `TimelineView` re-sorted its `columns` array alphabetically internally, wiping out the order from the parent. Moved the `columns.sort(localeCompare)` into the fallback branch only — the one that derives columns from appointments when no rota loaded. Parent's sourceList (with turn-order + drag override applied) is now preserved verbatim. **(2) SPA-CHECKOUT-V3 — six features (commit `feff907`).** (a) **🏷 Discount**: `bills.discount NUMERIC(10,2) + discount_reason TEXT` columns. `PUT /api/bills/:id/discount` endpoint. `total = subtotal - discount + tip` everywhere. UI: % presets (10/15/20) + custom £ + reason field on CheckoutScreen, shown above payment methods. (b) **📜 Legacy voucher** (no code): new button at top of voucher panel — closes bill as `voucher` without redemption record. For spas migrating from a previous EPOS with outstanding vouchers we don't track. (c) **Session voucher cross-treatment**: `vouchers.js` POST `/:id/redeem` relaxed — voucher for treatment A can now redeem against treatment B IF their `duration_minutes` matches. So "10 × 60-min Thai Massage" voucher works on any 60-min treatment. (d) **Timeline 22h grid**: `DAY_END` bumped 21→22 + `overflowY: 'auto'` on both desktop and mobile so the 20:00 row has visual room. (e) **Booking-source colour**: old `STATUS_STYLE`+`PAYMENT_STYLE` replaced with `SOURCE_STYLE` — colour by `appointment.source` not by lifecycle status. Indigo walkin/staff, blue online, green treatwell-full, amber treatwell-partial, grey cancelled, red no_show. (f) **Treatwell sub-type column**: `appointments.treatwell_payment_type TEXT` ('full' / 'partial'). Webhook reads it from payload or falls back to a name heuristic ("TW prepaid" → full). New `TreatwellTypeBlock` on the Edit modal for manual flip. **(3) Source dropdown on +New/Edit + editable voucher amounts (commit `2d68ea3`).** New picker on NewAppointmentModal: 📞 Phone (teal) · 🚶 Walk-in (indigo) · 🪷 Online (blue) · 🌐 Treatwell (yellow) — choosing Treatwell reveals Full/Partial toggle. New 'phone' SOURCE_STYLE entry (teal) so phone bookings are visually distinct from walk-ins. Status dropdown collapsed into a `<details>` summary. Backend `POST /appointments` accepts source + treatwell_payment_type with validation. Monetary vouchers now editable: new `MonetaryVoucherPanel` with amount input (default min(remaining,total)), Max button, live preview text. If amount=total → close bill as voucher; if amount<total → applied as discount with reason "Voucher SPA-XXX −£X", bill stays open. Same pattern for legacy voucher via new `LegacyVoucherPanel` — operator types £X, full closes the bill, partial records discount with "Legacy voucher −£X" reason. **(4) Source-change bugfix (commit `8b65be9`).** Korakot found that changing source from Treatwell to anything else didn't update the timeline colour. Two bugs: (i) `source` field was in the request body but missing from the SQL UPDATE — silent no-op; (ii) `treatwell_payment_type = COALESCE($N, treatwell_payment_type)` treated null as "preserve" → switching FROM Treatwell left the tw_type stuck. Fix: read current row first, compute new values explicitly (no COALESCE for either field), auto-clear tw_type when newSource !== 'treatwell'. E2E verified all 4 transitions (treatwell+partial → phone → treatwell+full → walkin). 17 files across the 4 commits, +700/−~120 net. | SPA-CHECKOUT-V3, SPA-DISCOUNT, SPA-VOUCHER-PARTIAL, SPA-SOURCE-COLOR |
| 2026-05-24 | Sam | **SPA-TURN-ORDER — receptionist-set timeline column order (commit `80a1fd4`).** Feedback from a potential client: "we want to set who's column 1 each day because therapists earn their turn by arriving first that morning". Existing drag-on-column-header saved per-date to localStorage — per-tablet, lost on cache clear. Now: a dedicated **🔢 Set turn order** button next to the view toggle in the timeline action bar. Opens a modal listing today's working therapists with a gold position number on the left, drag handle ⠿, name, plus ↑/↓ buttons for tap-precise reordering on mobile. **↻ Reset to alphabetical** to wipe; **Save order** persists to the server. **DB:** new `therapist_turn_order(date, therapist_id, position, set_by, set_at)` table with PK `(date, therapist_id)`. **Backend (`src/routes/therapists.js`):** `PUT /api/therapists/turn-order` body `{date, order:[ids]}` — admin/manager/reception roles allowed; DELETE+INSERT in a transaction. `DELETE /api/therapists/turn-order?date=…` clears. `/api/therapists/rota?month=` extended with `turn_order` array. Emits socket event `turn_order_updated` so other tablets re-render live. **Critical**: route ordering — both new endpoints MUST be defined BEFORE `PUT /:id` because Express was matching `/turn-order` as `id='turn-order'` on the first version and returning 500. Sort priority on the timeline: per-tablet localStorage override wins (existing), then server-saved turn_order for the date, then alphabetical fallback. E2E verified: PUT order [15,13,16] saves → /rota returns positions 1,2,3 → DELETE clears → missing date returns 400. 3 files, +221/−5. | SPA-TURN-ORDER |
| 2026-05-23 | Sam | **Reports v2 + amend bill method + treatment-swap propagation + Thai manual.** Four ships in one batch. **(1) Edit payment method on the timeline (commit `222e748`).** For "I tapped Cash but it was actually Card" mistakes. New `PUT /api/bills/:id/method` endpoint (admin/manager only) that re-runs all the validation POST /pay does — including deposit auto-credit and split-sum check. Frontend: new `PaymentEditBlock` on the appointment Edit modal, rendered when status is `completed`. Shows current method + Edit button → 4 colour-coded buttons (Cash / Card / 🎁 Voucher / 🌐 Treatwell). Inline "✓ updated" confirmation. Split bills point to Admin → Bills (inline split editing too complex for a timeline action). **(2) Reports v2 (same commit).** `/api/reports/therapist` now returns `minutes_worked`, `minutes_requested`, `requested_bookings` per therapist (where the customer specifically asked for that therapist — `therapist_requested = TRUE` — a loyalty/popularity metric) AND a same-range `by_payment_method` aggregate (split-aware via the same CTE pattern as trading/z-report). ReportsSection rewritten: **default = today** (not 30 days back), quick-button range presets (Today / Yesterday / Last 7 / This week / This month / Last 30 days), new colour-coded payment-method card grid, therapist table gains Hours worked + Requested hours (gold when non-zero) + % requested columns. Top-right Export CSV + Print buttons. **(3) Bill subtotal follows treatment swap (commit `39867f2`).** Korakot reported a £75 treatment swap to £65 where the bill still charged £75. SPA-PRICE-SNAPSHOT had captured price at the booking record correctly but didn't propagate to any already-open bill. Fix: PUT /api/appointments/:id now also UPDATEs `bills.subtotal + total` for the bill on that appointment, **only when** `payment_status != 'paid'`. Paid bills stay locked — they're a closed financial record. E2E: book £70 → bill £70 → swap to £65 → bill auto-updates to £65; pay it → swap back to £70 → bill stays £65. **(4) USER-MANUAL-TH.md + PDF pipeline (commit `3447aa1`).** Korakot asked for a Thai version of the user manual. 557-line mirror of USER-MANUAL.md, fully translated. Translation choice: UI labels (Cash, Booked, Override, Z-Report) kept in English so Thai operators see-read-map directly onto the English UI; Thai gloss in parens on first mention where helpful; brand names + currency untouched. PDF generation pipeline established: `npx marked --gfm < USER-MANUAL[-TH].md` → wrap in A4-styled HTML with brand colours (navy `#1e3a6e` headings + gold `#C9A84C` rules) — Thai version uses Sukhumvit Set / Thonburi font stack — Chrome headless `--print-to-pdf`. Both PDFs delivered to `~/Desktop/SiamEPOS-Spa-User-Manual.pdf` (914 KB) and `~/Desktop/SiamEPOS-Spa-User-Manual-TH.pdf` (786 KB). | SPA-REPORTS-V2, SPA-BILL-EDIT, SPA-BILL-SYNC, DOCS |
| 2026-05-22 | Sam | **SPA-PRICE-SNAPSHOT + USER-MANUAL.md + intensive E2E sweep.** Two ships after SPA-PAY-002. **(1) Price snapshot at booking time (commit `c9625eb`).** Korakot reported a £65 treatment charging £75 at checkout. Tracing the bills.js code surfaced a real design bug: `bills.subtotal` was read live from `treatments.price` at the moment the bill was created, so editing a treatment's price between booking and checkout retroactively changed the customer's bill. Fix: new column `appointments.price_at_booking NUMERIC(10,2)`, populated at every booking site — widget POST /book, admin POST /appointments, treatwell webhook, and admin PUT /appointments/:id (only re-snapshots when the treatment is swapped on edit; pure status/notes edits leave it alone). `bills.js` POST / now reads `COALESCE(a.price_at_booking, t.price)` — snapshot wins; live treatment price only used as a belt-and-braces fallback. Migration backfills existing rows with the current treatment price (= the value the bill would have computed under the old code path, so zero behavioural change for past bookings; locked-in pricing for everything new). E2E verified: book Aromatherapy at £65 → edit treatment to £999 → create bill → subtotal £65 (not £999). 6 files. **(2) USER-MANUAL.md (commit `e8ae7e0`).** 547-line complete manual for spa owners + staff. 15 sections covering: getting started + first-day checklist, front desk daily ops (timeline colours, booking, status), checkout & payments (single + split with voucher), online bookings (widget embed, deposit policy), customer self-service portal (magic-link), phone bookings + payment links (SPA-PAY-002), Treatwell integration, staff management (PINs / roles / rota / overrides), treatments / rooms / categories, Clients (CRM) + medical questionnaire + GDPR erasure, gift vouchers (monetary + sessions), email campaigns + unsubscribe, reports (trading / Z-Report / CSV / print / therapist), settings, and 8-item troubleshooting section. Plus quick-reference daily checklist (open / during / end-of-day). Markdown — renders in any editor or GitHub; converts to PDF cleanly for printed handover. **(3) Intensive E2E sweep.** 44 automated checks against the local backend across 14 blocks: auth, public widget, settings round-trip, rota + override, deposit guard, rota enforcement (London TZ), manage-by-token (HMAC), deposit auto-credit on bill close, vouchers (monetary + sessions), reports (trading + Z-report + therapist with new identity/voucher_sales fields), clients enrichment + search, SPA-PAY-002 endpoint, Treatwell webhook guard, static portal serving. **35 ✅ · 0 real bugs.** 9 ❌ were all explainable test-data issues: 7 cascaded from one slot-collision (same date+time reused across runs), 1 wrong assumption (voucher lookup requires auth — by design), 1 follow-on. Core code is solid. | SPA-PRICE-SNAPSHOT, DOCS |
| 2026-05-22 | Nook | **SiamEPOS Full Production Smoke Test ✅ 49/51.** 20 sections, 51 automated checks against `restaurant-epos-production.up.railway.app`. All core trading flows healthy: orders, menu, billing, reports, takeaway, CRM, Z-report, inventory (Pro plan), clock records, delivery, dining tiers, table combos, widgets. **2 failures:** (1) BUG-EPOS-001 — `GET /api/reservations/settings` returns HTTP 500 (deprecated column names from pre-SEPOS-049 schema — fix brief in Handoffs for Krit). (2) POST /api/reservations 400 — test script used legacy field names; API is correct, test corrected for future runs. **2 warnings:** (a) ⚠️ SECURITY — PIN "0000" accepted on production (trivially guessable; Korakot to audit in Admin → Staff). (b) `/api/validate-manager-pin` 404 — invented endpoint; PIN gate is inline in DELETE /api/orders/:id, not a bug. **Cleanup needed:** test order #243 left open in production (see Handoffs). Report: SiamEPOS-Smoke-QA-Report.docx | EPOS-SMOKE |
| 2026-05-22 | Sam | **SPA-PAY-002 + Z-Report polish + deposit-visibility round.** Four follow-up commits after the SPA-PAY-001 ship. **(1) Deposit badges across the missing surfaces (commit `be47bfb`).** SPA-PAY-001 originally surfaced the deposit only on Checkout / Online Booking tab / Trading report — Korakot pointed out the appointment edit modal, client history, and bills list didn't show it. Added: (a) `NewAppointmentModal` — green banner at top in Edit mode when `deposit_amount > 0`, status-suffixed ("balance due at till" / "refunded" / "forfeit" / "bill closed"). (b) `ClientProfileScreen` — small green pill on each history row "🌐 Online · £25 deposit". (c) `BillsSection` — split bills now show the underlying breakdown inline under the method label (`🌐 deposit £25 / 💵 cash £45`). (d) `ZReportSection` + reports.js z-report — relabels the 'deposit' row as "🌐 deposit (online)" and adds a complete "Online deposits (Stripe)" block (taken / refunded / pending / consumed / forfeit). **(2) Z-Report export + per-bill detail (commit `6f2881a`).** Added 📥 Export CSV button + 🖨 Print button to the Z-Report screen. CSV is line-by-line: one row per bill with per-method columns (Cash / Card / Voucher / Deposit / Treatwell) auto-populated from `split_payments[]`, so an accountant can sum a column in Excel and read each method's total straight off. Summary footer includes totals + by-payment-method + online-deposit Stripe activity. BOM-prefixed UTF-8 so Excel renders £ correctly. New `@media print` block in styles.css strips nav/sidebar/buttons/modal backdrops, widens `.admin-layout`, keeps cards `page-break-inside-avoid`. Also added a bills-detail table to the Z-Report card (same data the CSV exports) so the on-screen view is scannable + the print copy carries the bills. **(3) SPA-PAY-002 — payment-link for phone/WhatsApp bookings (commit `46e80c9`).** Use case Korakot flagged: customer rings up wanting a specific therapist + treatment + time and wants to pay immediately without going through the website widget. Now: receptionist creates the booking in admin, taps "💳 Request deposit by link" on the appointment Edit modal (new `PaymentLinkBlock` component, shown when no deposit + policy != 'none'), backend `POST /api/appointments/:id/payment-link` creates a fresh Stripe PI + flips `payment_status='deposit_pending'` + returns the manage-link URL. Receptionist gets a Copy / 📱 WhatsApp (wa.me deep-link, prefilled text) / ✉️ Email (mailto with prefilled body) row. Customer taps link → opens `my-booking.html` which now detects `deposit_pending` and renders a Pay Now panel with Stripe Elements PaymentElement (tabs layout). After confirmCardPayment, frontend calls new public `POST /by-token/:token/confirm-payment` which re-verifies the PI with Stripe server-side before flipping to `deposit_paid`. The same URL serves Pay Now → Manage → Cancel-with-refund as the booking evolves. **(4) Z-Report shop identity + voucher sales (commit `32d11c0`).** Added: spa identity header (centred Georgia heading + address/phone/email line) on the Z-Report card / CSV first rows / print view — backend loads `settings.spa_name/spa_email/spa_address/spa_phone` with env-var (`SPA_NAME`/`SPA_EMAIL`/`SPA_ADDRESS`) fallback. Voucher sales section in z-report endpoint + screen + CSV summary (same shape the trading endpoint already returned, count / total / by_payment_method). **Files across all 4 commits:** 17 files, +~700/−~10. All E2E-verified locally. **Still requires Railway env vars** for SPA-PAY-002 to actually generate links: `STRIPE_PUBLISHABLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `BOOKING_SECRET` (same as SPA-PAY-001). Without them, the "Request deposit by link" button returns 503 cleanly; everything else works. | SPA-PAY-001, SPA-PAY-002 |
| 2026-05-22 | Krit | **CSV export on every admin report.** Audit: 3 sections already had CSV (VAT, Clock, Customers); **8 didn't**. Added Export CSV to all the missing ones via a shared util (`client/src/utils/csv.js`, exports `downloadCsv(filename, rows)`), so every report-style admin screen now hands the operator a one-click CSV that respects whatever filter / date range they're viewing. Filenames carry the relevant context so downloads don't overwrite (`bills_2026-05-22_to_2026-05-22_Cash.csv`, `staff-performance_2026-05-18_to_2026-05-22.csv`, `stock-movements_2026-05-22_waste.csv`, etc.). Sections newly exportable: **Reports** (sales + items, per-period), **Bills** (date range + payment method), **Staff Performance**, **Z-Report** (history + per-report final on step 4 alongside Print), **Inventory → Cost vs Sales** (P&L + wastage + expenses in one bundle), **Inventory → Stock Movements** (respects type filter), **Campaigns** (sent history), **Allergen matrix** (UK14 grid with Y/empty cells). Commit `dcd6390`, 9 files, +266 lines, build clean. The 3 pre-existing exports still use their own inline copies — left alone to avoid risk; minor future DRY cleanup. | REPORTS-CSV |
| 2026-05-22 | Nook | **SPA-PAY-001 QA ✅ — Online Booking Management.** 33 automated checks (16 blocks: deposit settings, stripe-config public endpoint, payment-intent 503/400 error guards, deposit guard on widget book, booking token HMAC, customer portal GET/PUT/DELETE by-token, amendments audit trail, reports trading online_deposits block, bill pay, static file serving, appointments source filter). Sam E2E 6/6: full £25 Stripe deposit → £70 checkout auto-credited as deposit £25 + cash £45 split → appointment.payment_status=fully_paid → trading report correctly splits Stripe vs till revenue. Zero server bugs. 7 manual checks pending (live Stripe Elements, confirmation email manage-link, cancel-refund inside/outside window, admin Refund button). Test script: spa-epos/test-spa-online-booking.js. Report: SiamEPOS-SPA-PAY-001-QA-Report.docx | SPA-PAY-001 |
| 2026-05-22 | Sam | **SPA-PAY-001 — Online booking deposit (Stripe) + customer self-service amendments + Online Booking admin manager.** Full end-to-end online-deposit flow shipped in one bundle (commit `ff51c20`, 13 files, +1512). **(1) Widget Stripe deposit.** Lazy-loads Stripe.js, new step 5 "Pay deposit" between Details and Confirmation. `POST /api/widget/payment-intent` creates a Stripe PI for the quoted booking; appointment is NOT created until the PI verifies as `succeeded` server-side, so abandoned payments never leave phantom appointments. Reads `amount_received` from Stripe to defend against client tampering. Graceful fallback: if `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` aren't set on Railway, the widget books without deposit so spas without Stripe configured still work. 4 deposit models supported: `none` / `fixed_amount` (default £25) / `percentage` / `full_prepay`. **(2) Customer self-service portal.** `client/public/my-booking.html` is a mobile-first static page served from `spa-api.siamepos.co.uk/my-booking.html?token=…` (HMAC-signed appointment-id token, separate `BOOKING_SECRET` from `UNSUB_SECRET`). No login. Reschedule (slot picker re-uses `/api/widget/availability`, server revalidates rota + availability before saving) and Cancel (inside policy window = Stripe refund issued + `payment_status='refunded'`; outside = forfeit, customer told why in the email). All amendments append to a new `appointment_amendments` audit table with `by_customer=TRUE`. **(3) Email templates.** `sendBookingConfirmation` extended with deposit / balance / "Manage your booking" gold CTA; new `sendBookingRescheduled` (old/new times with strike-through) and `sendBookingCancelled` (refund or forfeit explainer). Branded navy/gold layout matching the voucher gift email. **(4) Checkout deposit credit.** `CheckoutScreen` shows a green "💳 Deposit paid online −£25" line + gold "Balance due now £30" pill when `appointment.deposit_amount > 0`. Split modal opens with `balance` as the total to split. Backend `POST /api/bills/:id/pay` auto-credits the deposit: if a bill closes with a deposit on the appointment, the chosen method (cash/card) is converted to `'split'` and a `'deposit'` row is prepended to `split_payments`. Customer-built split payments are revalidated against the balance, not the full total. Stops bills showing £55-cash when the customer really paid £25 deposit + £30 cash. **(5) New 🌐 Online Booking admin tab** (`OnlineBookingSection.jsx`, mounted under Settings in `AdminScreen` NAV). Top: policy settings card with 4-button deposit-model picker, amount/percentage/cancel-window/policy-text fields (save on blur/click). Middle: stats strip (Upcoming online / Deposits held £ / Cancelled). Bottom: bookings list filtered to `source='online'` with client + therapist + status pill + Stripe-dashboard deep-link + Refund action. **(6) Reports.** `/api/reports/trading` adds an `online_deposits` block (count_pending / count_consumed / count_refunded / count_forfeit + total_taken / total_refunded). TradingSection renders "🌐 Online deposits — settled to Stripe, not the till" card so the owner doesn't double-count Stripe revenue against till cash. **DB migrations on first boot:** `appointments.deposit_amount NUMERIC(10,2)`, `deposit_stripe_id TEXT`, `payment_status TEXT` (none/deposit_paid/refunded/forfeit/fully_paid); new `appointment_amendments` table + index; 5 default settings keys (deposit_model='fixed_amount', deposit_amount='25', deposit_percentage='25', cancel_window_hours='24', cancel_policy_text=…) seeded via `ON CONFLICT DO NOTHING`. **E2E verified locally:** settings table loads keys cleanly; widget `/stripe-config` returns publishable_key+policy publicly; `/payment-intent` 503s cleanly without Stripe creds; bookingToken round-trips (GET `/api/booking/by-token/:token` returns booking + policy + editable flag; bogus tokens 401). **Required Railway env vars to enable real Stripe deposits:** `STRIPE_PUBLISHABLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `BOOKING_SECRET` (`openssl rand -hex 32`). Without them, widget falls back to no-deposit booking — safe. The admin tab works immediately for editing policy. **Stripe webhook URL to register:** `https://spa-api.siamepos.co.uk/api/stripe/webhook` (event `payment_intent.succeeded` — endpoint already wired). | SPA-PAY-001 |
| 2026-05-21 | Krit | **Baan Siam multi-tenancy bug — widget bookings invisible in admin.** Two compounding root causes. **(1) `RESTAURANT_ID` env var was unset on Baan Siam's Railway service** → backend defaulted to `'siamepos'` → widget POSTs correctly tagged bookings as `restaurant_id='baan-siam'` but the admin's `GET /api/reservations` filtered by env-default `'siamepos'` → invisible. Korakot set `RESTAURANT_ID=baan-siam` on Railway + ran the migration SQL (`UPDATE reservations/orders/order_items/payments/menu_items/categories/staff/tables/settings SET restaurant_id='baan-siam' WHERE restaurant_id='siamepos'`). **(2) `ReservationsScreen.jsx` + `ReservationPlanView.jsx` each inlined their own `getServerURL` that hardcoded `restaurant-epos-production`** — bypassing `VITE_API_URL` — so the Reservations tab on `app.baan-siam.siamepos.co.uk` (per-client Netlify deploy) silently queried the MAIN backend regardless of build env. Most other screens were fine (they import the canonical `SERVER_URL` from `api.js`). Fixed in **7f33266** — both screens now import from `'../api'`; `utils/serverDetect.js` cloud-probe URL also respects `VITE_API_URL`. **Prevention for future clients:** `scripts/provision-client.sh` hardened in **4f13cd9** — verifies `railway variables` shows `RESTAURANT_ID=<slug>` immediately after `variables set`, and post-deploy curls `/api/restaurant` to confirm the live deployment identifies as the right slug, with loud warn + remediation command if not. **(3) The deployed siamepos.net mockup site had a stale `SW_RID='siamepos'`** in its booking widget — fix was committed in `6b79bed` but never deployed because that Netlify site is **drag-and-drop, NOT git-connected** → `git push` doesn't propagate. So every new widget booking was born tagged `'siamepos'` on Baan Siam's DB and invisible to the `baan-siam`-filtered admin (the socket notification still fired globally, but the filtered re-fetch dropped the row — exactly the "notification but no row" symptom we hit). Korakot redeployed siamepos.net via drag-and-drop of `client/MockUp Website/` + re-ran the migration SQL one more time. End-to-end verified after. **Lessons for next time:** (a) When chasing a "data missing" bug across multiple deployments, **curl the LIVE asset and compare with the repo** — would have caught the SW_RID staleness in the first 5 minutes. (b) Migration SQL *camouflages* symptoms by relabeling old rows; it doesn't fix the source. Always make a NEW write after a fix to verify the loop is closed end-to-end (not just look at re-tagged existing rows). (c) **Hook siamepos.net to git** so future fixes auto-deploy — Netlify → Site settings → Build & deploy → Link site to Git, base directory `client/MockUp Website`. | BAAN-SIAM-FIX |
| 2026-05-21 | Sam | **SPA-CRM-001 + SPA-003 — Clients/CRM tab + Treatwell integration.** **SPA-CRM-001 (Clients/CRM):** new `👤 Clients` tab in Admin between Bills and Vouchers — VIP/Regular/New/Lapsed status pills, search + filter, marketing-consent toggle, CSV export, opens existing ClientProfileScreen on row click. The top-level Clients screen (EPOS) now also surfaces status pill + visits + lifetime spend + last-visit on each list card. Spa's `GET /api/clients` enriched with `total_visits`, `first_visit`, `last_visit`, `total_spend` derived from real joins (`appointments → bills` by `client_id` and `appointment_id`) — accurate, not the EPOS's heuristic table+date join. Same status thresholds as EPOS so both products read identically to the operator. **SPA-003 (Treatwell):** (1) `appointments.treatwell_booking_id TEXT` + unique partial index for webhook dedup. (2) `POST /api/treatwell/webhook` gated by `TREATWELL_WEBHOOK_SECRET` header — assumed payload shape documented in `src/routes/treatwell.js` (booking_id / status / scheduled_for / customer{name,phone,email} / service{name,duration_minutes,price}). Maps treatment by case-insensitive name with ILIKE fallback; tags unmatched with `[unmatched treatment: x]` in notes. Status=cancelled flips the matched appointment to cancelled. (3) Bills validates `'treatwell'` as a payment method — Treatwell collects, settles minus commission; spa marks bill paid without recording cash. (4) `/api/clients` returns `acquisition_source` = source of the client's earliest non-cancelled appointment. (5) `/api/reports/trading` returns `by_source` block (appointments + revenue split). (6) ClientsSection gets a 🌐 Treatwell pill + source filter (treatwell / direct / online / walk-in / staff) — Export CSV includes source so the owner can target Treatwell customers with a direct-booking offer via their existing email tool. (7) CheckoutScreen renders a 🌐 Treatwell payment button conditionally (only when `appt.source === 'treatwell'`), with a tooltip explaining commission semantics. (8) ReportsSection adds a "Booking source — {date}" card with one tile per source (label / appointments / revenue). **Bonus fix:** patched a pre-existing PG syntax bug in `/reports/trading` — `COUNT(*)::int FILTER (...)` is invalid syntax; the cast must wrap the filtered aggregate. Found when my new ReportsSection fetched the trading endpoint for the first time. **E2E verified locally:** webhook with secret → appointment + client created (treatment matched) → re-send dedups → /api/clients returns the client with `acquisition_source: 'treatwell'` → cancellation flips status. **Scope cap:** full in-app Brevo campaign sending de-scoped — SiamSpa has no email-sending system yet, so owner exports filtered Treatwell customers as CSV and sends via their email tool. Brevo integration is a separate ticket. **Status:** implemented locally, needs commit + push + Railway boot (migrates `treatwell_booking_id` + index) + production env var `TREATWELL_WEBHOOK_SECRET` set + Treatwell portal registered the webhook URL `https://spa-api.siamepos.co.uk/api/treatwell/webhook`. | SPA-003, SPA-CRM-001 |
| 2026-05-21 | Sam | **SPA-DEMO-001 + SPA-DEMO-001-PREP — Baan Siam Spa demo website.** Sister to the restaurant Baan Siam demo at siamepos.net, targeting www.siamepos.com to demo the SPA-002 therapist-picker pitch to prospects. **PREP (backend + widget):** `ALTER TABLE therapists ADD COLUMN IF NOT EXISTS photo_url TEXT` (next to `specialisms`); `GET /api/widget/therapists` now returns `photo_url`; `src/routes/therapists.js` SELECT/POST/PUT extended; `TherapistSection.jsx` gains a Photo URL input + live preview; widget step 2 renders `<img>` when `photo_url` is present, initials fallback otherwise; `SiamEPOSSpa.open({ treatmentId, therapistId })` pre-select API ships — guards against MouseEvent (auto-mount keeps working). **Demo site (`spa-epos/mockup-website/`):** 5 pages — `index.html` (lotus hero / CTAs / 3 feature cards / founder pitch), `treatments.html` (live fetch from `/api/widget/treatments`, grouped by category, "Book this →" deep-links), `therapists.html` (THE VDIT-counter — live profiles with photo + specialisms + "Book with {first name} →" deep-link), `about.html`, `contact.html` (47 Charlotte Street W1T 4ED · 020 7935 0044 · embedded Google map iframe). Shared `assets/css/main.css` reuses SPA-UI-001 brand system (navy `#1e3a6e` / gold `#C9A84C` / cream `#faf7f2` / Playfair + Inter + Georgia / lotus SVG). `assets/js/config.js` auto-points the widget at `http://localhost:5050` for local dev and `spa-api.siamepos.co.uk` for production, with `?api=` URL override. Mobile-first (44px tap targets, 16px inputs, hamburger nav under 768px). `netlify.toml` + `README.md` with deploy guide. **Seed script:** `scripts/seed-demo.js` — idempotent populator (3 rooms · 6 categories · 10 treatments · 4 Thai therapists with Unsplash photo URLs + specialisms). Verified end-to-end against the local backend — login + 5 phases all green, `/api/widget/therapists` returns Anong / Malee / Niran / Suthida with photos. **Status:** implemented locally, needs commit + push, new Netlify site set up at www.siamepos.com (Namecheap DNS — CNAME `www` + ALIAS apex), seed script re-run against production once spa-api goes live. **Cleanup note for Korakot:** local DB has leftover therapists from earlier test scripts (Admin, Fah, Kot, Nan) — deactivate them in Admin → Therapists before the demo site goes live or they'll appear on therapists.html. | SPA-DEMO-001 |
| 2026-05-21 | Sandy | **SPA-UI-002 — SiamSpa admin UI brand overhaul.** All 13 admin section pages now match the SiamEPOS brand CI. Added to `styles.css`: `button.gold`, `button.outline`, global table styles (`thead th` uppercase caps, `tbody tr` hover tint), `.stat-card` (navy top-border accent, large stat value), `.section-header` (Playfair Display h2 + muted subtitle, 2px border-bottom), `.tab-strip` (gold underline on active tab). Updated `TradingSection.jsx`: stats now use `.stat-card` class with navy values. Fixed `NewAppointmentModal.jsx`: client-selected panel changed from Bootstrap blue to brand gold-light; hover tint changed from `#f0f9ff` to `navy-muted`. Fixed `CheckoutScreen.jsx`: Voucher + Redeem buttons use `className="gold"` instead of inline hex. Added `.section-header` with h2 heading + subtitle to all 13 admin sections: Settings, Reports, TreatmentMenu, Bills, Therapists, Vouchers, Clients, Campaigns, Rooms, ZReport, Rota, BookingSettings, EmbedCodes. Section-specific detail in right slot where useful (ZReport: date picker; Rota: tab buttons). Needs `git push`. | SPA-UI-002 |
| 2026-05-25 | Sandy (Cowork) | **UX fix — login screen layout shift on wrong PIN.** Both restaurant EPOS (`client/src/screens/LoginScreen.jsx`) and SiamSpa (`spa-epos/client/src/screens/LoginScreen.jsx`). Error message was conditionally rendered between PIN dots and numpad, pushing the pad down on wrong PIN entry. Fixed by replacing conditional render with an always-present fixed-height (38px) slot — numpad stays locked in place regardless of error state. Commit `4e505fa`. Korakot to `git push`. | LOGIN-UX-FIX |
| 2026-05-25 | Sandy (Cowork) | **Nav height toggle fixed across siamepos.co.uk.** `lite.html` and `website-design.html` had `padding:12px 60px` vs `padding:18px 60px` on all other pages — caused ~12px nav height jump when navigating between pages. Normalised to `padding:18px 60px` across all 7 pages. Committed. Korakot to `git push`. | MARKETING |
| 2026-05-21 | Sandy (Cowork) | **SiamSpa on siamepos.co.uk marketing site.** Created full `spa.html` dedicated landing page (hero "Your Thai spa. Fully managed. From your phone.", 6 feature cards, HTML/CSS timeline mockup, before/after comparison, £49/mo pricing). Added SiamSpa nav link (`<li><a href="spa.html">SiamSpa</a></li>` after SiamLite) to all 7 navigated pages: index.html, features.html, pricing.html, lite.html, why-siamepos.html, website-design.html, book-demo.html. Added SiamSpa teaser section to `index.html` homepage (navy dark-bg section between pricing and trust bar — gold CTA "Learn about SiamSpa →"). Added SiamSpa to footer Product column in index.html. Added `spa.html` to sitemap.xml (priority 0.9). **Commit needed by Korakot:** `rm -f .git/HEAD.lock .git/index.lock && git add -A && git commit -m "MARKETING: SiamSpa full landing page + homepage teaser + nav on all pages" && git push` | SPA-MARKETING-001 |
| 2026-05-21 | Krit (Cowork) | **SiamSpa timezone + card payment fixes.** (1) Card payment no longer routes through Stripe — clicking Card at checkout now calls `POST /api/bills/:id/pay` with `method:'card'` directly, same as Cash. Removed `StripePayModal` import + `showStripe` state + Stripe note from CheckoutScreen.jsx. (2) Timezone fix: appointments booked at e.g. 11:00 were displaying as 12:00 (BST offset not applied). Fixed `starts_at` construction in NewAppointmentModal to use `new Date(\`\${date}T\${time}:00\`).toISOString()` (local→UTC conversion). Fixed `initDate` + `applySlot` to use local date components (`getFullYear/getMonth/getDate`) instead of `toISOString().slice(0,10)` (UTC). Fixed `todayISO()` in AppointmentScreen to use local date. | SPA-TZ-001, SPA-CARD-001 |
| 2026-05-20 | Krit (Cowork) | **SiamSpa UI/UX brand overhaul (Sandy-led).** TopNav → dark slate-navy `#1e3a6e` bar with lotus SVG badge, Georgia "SiamEPOS Spa" wordmark, gold active nav links. LoginScreen → full navy background, lotus logo, gold PIN dots with glow. AdminScreen → left sidebar matching EPOS admin: `#1e3a6e`, gold active state, 200px wide. `styles.css` → full brand system (navy/gold vars, Playfair Display + Inter). AppointmentScreen → timeline header + action bar `#7a4f1e` → `#1e3a6e`; COL_COLORS → navy shades; sub-text + badge → gold. Rota-aware timeline: only shows therapists working that day. Click-to-book on empty slots, double-click to edit. Full-height timeline (no vertical scroll). Needs `git push`. | SPA-UI-001 |
| 2026-05-19 | Sam | **BUG-SPA-001 ✅ fixed.** `src/routes/clients.js` PUT /:id/medical INSERT path now wraps the NOT NULL placeholders in COALESCE defaults — booleans (pregnancy, heart_condition, diabetes, epilepsy, cancer, dvt, recent_surgery, bone_fracture, skin_condition, varicose_veins, osteoporosis, lymphoedema) fall back to `FALSE`, blood_pressure falls back to `'none'`. Nullable text columns (medications, allergies, areas_to_avoid, skin_conditions_detail, digital_signature) left as plain `$n`. UPDATE path was already COALESCE-safe so untouched. Re-ran `node test-spa.js` against the local backend → **55/55 ✅ 0 failed 0 warnings.** Implemented locally — needs commit + push for Nook to re-verify on live spa-api. | BUG-SPA-001 |
| 2026-05-19 | Nook | **SPA-001 QA ✅ 55/55 CLEAN** — Full backend sign-off. Auth, treatments, rooms, therapists, clients, medical questionnaire (create + upsert + digital signature), appointments, conflict detection (therapist + room), availability, bill lifecycle (create → tip → pay → auto-complete), GDPR erasure — all 13 blocks pass. BUG-SPA-001 (medical INSERT NOT NULL cols) found and fixed by Sam within same session. 5 manual UI checks remain. Report: SiamEPOS-Spa-QA-Report-May2026.docx | SPA-001 |
| 2026-05-19 | Sam | **SPA-002 — Public booking widget + therapist selection.** Direct answer to VDIT's cold-calling pitch. The scaffold already had a 5-step widget + /treatments, /availability, /book endpoints; the gap was: (1) no `GET /api/widget/therapists`, (2) step 3 of the widget was a placeholder ("Phase 1 simplification"), (3) no `specialisms` column on therapists, (4) `POST /book` didn't return therapist_name / room_name, (5) no admin embed-code page, (6) no auto-mount on `<div id="siamespa-booking">`. **Backend:** `ALTER TABLE therapists ADD COLUMN IF NOT EXISTS specialisms TEXT` in `src/db/database.js` (re-runs safely on boot); new `GET /api/widget/therapists` returning {id, name, specialisms}; `POST /api/widget/book` now JOINs and returns `therapist_name` + `room_name` on the appointment; email relaxed to optional (Brevo still fires if given); marketing_consent fixed to additive (`OR` instead of overwrite) so re-bookings don't accidentally opt customers out. `src/routes/therapists.js` SELECT/POST/PUT extended for specialisms. `src/server.js` adds `/booking-widget.js` alias alongside the existing `/widget.js` so both embed patterns work. **Widget (`client/public/widget.js`):** step order matches SPA-002 spec (Treatment → Therapist → Date+Time → Details → Confirmation); step 2 is a real picker with circular initials avatar, name, specialisms (or 'Available' if blank), and "Any available" as the marked first card; therapist_id flows through to `/availability` so slots filter correctly when picked; confirmation card shows Therapist + Room; auto-mounts a styled "Book your treatment" button into `<div id="siamespa-booking"></div>` if present, while still exposing `SiamEPOSSpa.open()` for custom buttons. **Admin:** new `EmbedCodesSection.jsx` (Embed Codes tab) shows both embed snippets with one-click copy and an editable API origin; old duplicate snippet card removed from `SettingsSection.jsx` to keep a single source of truth; `TherapistSection.jsx` gains a Specialisms input + column. **Status:** implemented locally, every touched `.js` parses (`node --check` clean), needs commit + `git push` + Railway boot to migrate `therapists.specialisms`. Korakot to push, then handoff to Nook for live QA (steps in Handoffs). | SPA-002 |
| 2026-05-19 | Krit | Reservations — multi-table join + date-navigable floor plan. New `table_ids` column on reservations (CSV; `table_id` stays the primary; PG + SQLite). `PUT /api/reservations` accepts a `table_ids` array. Booking panel table picker is now multi-select — tap tables to join them, live "seats X of Y" summary, 🔗 linked combos as one-tap shortcuts; timeline + floor plan render a booking across every table in its set. Floor plan un-pinned from today (reverses the SEPOS-044 pin) — gets the timeline's date picker; walk-ins restricted to today (non-today dates hide the walk-in button + offer pre-assign instead). Commit 8e06d56, `table_ids` migrates on Railway boot. | RESERVATIONS |
| 2026-05-19 | Krit | SEPOS-DELIVERY-001 kitchen UX — dispatched-courier status is now a compact rounded pill (🛵 + friendly label, green when delivered) + small "Track" link, replacing the cluttered one-line text (Sandy's issue #3; issues 1/2/4 — address strip, 18px header, card density — already done by the concurrent session). Commit ab492f6. | SEPOS-DELIVERY-001 |
| 2026-05-18 | Nook | Printer Setup Guide ✅ — Bilingual Word doc (English + Thai). Covers: hardware (cnfujun 80mm), USB + Ethernet setup, driver gotcha (POS-80 not Generic PostScript), SiamEPOS network printer config, silent vs popup printing (local URL vs cloud URL), troubleshooting table. File: SiamEPOS-Printer-Setup-Guide.docx | SEPOS-025/026 |
| 2026-05-18 | Nook | SEPOS-049 + SEPOS-050 QA ✅ 23/23 CLEAN — Party size cap (max 8), availability blocking, staff override uncapped, phone in rejection message, past date guard, full CRUD all passing. 5 manual checks remain (Timeline UI + widget stepper). Report: SiamEPOS-Reservations-QA-Report-May2026.docx | SEPOS-049, SEPOS-050 |
| 2026-05-18 | Nook | SEPOS-DELIVERY-001 Courier Dispatch QA ✅ 20/21 — Stuart dispatch, double-dispatch block, webhook lifecycle (in_progress + delivered + unknown), re-dispatch after cancellation, all edge cases passing. "delivered" webhook auto-closes order. 6 kitchen UI manual checks remain for Krit. Orders 227-229 need manual delete in Admin → Bills. Report: SiamEPOS-CourierDispatch-QA-Report-May2026.docx | SEPOS-DELIVERY-001 |
| 2026-05-18 | Nook | SEPOS-DELIVERY-002 QA ✅ 23/23 CLEAN — delivery test script (test-delivery.js) run 3 times, all issues resolved. Stuart quote (£15.28), dispatch, tracking URL, delivery_address storage, CRM integration, order validation all passing. 8 manual checks documented (widget toggle, address UX, kitchen 🚗 label, Admin delivery settings). Test orders 216 + 217 need manual delete from Admin → Bills (delivery orders stay open until courier delivers — expected). Full report: SiamEPOS-Delivery-QA-Report-May2026.docx | SEPOS-DELIVERY-002 |
| 2026-05-18 | Korakot + Claude | SEPOS-DELIVERY-001 bug fixes — (1) `SELECT name` → `SELECT item_name AS name` on order_items in dispatchCourier. (2) Stuart sandbox hostname corrected to `api.sandbox.stuart.com`. (3) Sandbox credentials updated in Railway. (4) Stuart eta object guard (only store string etas). (5) client_reference now includes timestamp to allow re-dispatch. (6) Done tab Collected button hidden for already-collected orders (takeaway_status check). Stuart dispatch confirmed working end-to-end — "finding rider" status + tracking link live. | SEPOS-DELIVERY-001 |
| 2026-05-18 | Korakot + Claude | SEPOS-DELIVEROO-001 — deliverooService.js built (OAuth2, acceptOrder, markReady, parseWebhook). POST /api/deliveroo/webhook endpoint in server.js: deduplicates by deliveroo_order_id, creates takeaway order, auto-accepts back to Deliveroo, fires socket event to kitchen. POST /api/deliveroo/ready/:orderId marks order ready + notifies Deliveroo. deliveroo_order_id column added to orders table. Webhook URL registered in Deliveroo portal. Sandbox site "Baan Siam" created — pending activation for test order. | SEPOS-DELIVEROO-001 |
| 2026-05-18 | Krit | SEPOS-STRIPE-001 — Stripe subscription billing webhook. New `POST /api/stripe/webhook` in `src/server.js`: raw body parser registered before the global `express.json()` so signature verification gets the unparsed body. `customer.subscription.created/updated` map the Stripe price ID → `restaurants.plan`; `.deleted` → plan/status `'suspended'`; `invoice.payment_failed` stamps a new `payment_failed_at` column (added to PG + SQLite). Handler errors still return `200 {received:true}` so Stripe doesn't retry. Installed the `stripe` SDK. Webhook needs only `STRIPE_WEBHOOK_SECRET` — `constructEvent` is pure HMAC, so `STRIPE_SECRET_KEY` is NOT required (removed that dependency after it 500'd on the restaurant-epos service). Pushed bd3248e + 5d742db, route live on Railway; `STRIPE_WEBHOOK_SECRET` added to the restaurant-epos service. Signed off by Nick. ⚠️ Note for Lite billing: the webhook updates `restaurants.plan` on whatever DB its deployment uses — under Model A each Lite client has its own DB, so a Lite subscription event hitting the main service won't find a row (handler logs + still 200s). Cross-deployment plan routing is an open question for whoever wires Lite Stripe billing. | SEPOS-STRIPE-001 |
| 2026-05-18 | Krit | SEPOS-049 — Reservations Timeline linked-table fix: each table is now its own row, a small party no longer consumes the whole linked group (spanning bookings show a dashed "ghost" on sibling rows). SEPOS-050 — per-restaurant online booking party-size cap: `max_party_size` (default 8) + `restaurant_phone` on restaurant_settings; widget guest stepper caps at the limit + "call us" note; availability + POST guards (staff-created bookings uncapped). Pushed (8eb1ed8, e30eb77), columns migrated + verified live on Railway. 'siamepos' restaurant_phone set to 020 7935 0000. Party-of-9 rejected end-to-end (8 OK, 9 blocked). | SEPOS-049, SEPOS-050 |
| 2026-05-14 | Krit | VAT per item, staff clock-in/out, void types, resend reasons, multi-device setup, staff performance report, wastage cost, auto stock depletion, Customer CRM + Brevo campaigns, Make.com webhooks, takeaway widget | SEPOS-021 to 034 |
| 2026-05-14 | Krit | Bidirectional active-order sync, realtime cloud→Mac relay, kitchen direct mode, timezone fix, Pass tab cross-browser fix | SEPOS-PRO-002, PRO-003 |
| 2026-05-14 | Pose | Finance page built (Starling Bank API integration, P&L, AI summary) | Back Office |
| 2026-05-14 | Claude | Back Office CLAUDE.md, Spa CLAUDE.md (Sam), QA CLAUDE.md (Nook), Agent Guide updated | Setup |
| 2026-05-15 | Krit + Nook | SEPOS-046 — Invoice scanner now processes line_items: stock update, cost update, price change detection, auto-create new ingredients. 52/53 tests passed. | SEPOS-046 |
| 2026-05-15 | Krit + Nook | SEPOS-043 — Role-based access. Backend 23/23 tests passed. Supervisor blocked from closed bill delete. Admin/Manager unaffected. 3 manual frontend checks remain. | SEPOS-043 |
| 2026-05-15 | Claude | Created TEAM-STATUS.md shared board, CLAUDE.md files for all 10 agents (Krit, Sam, Pose, Nook, Sandy, Kai, Maya, Nick, Nong + Krit-Cowork), auto-trigger end-of-session phrases added to all files | Team Infrastructure |
| 2026-05-15 | Nook | QA Report (Word doc) covering SEPOS-046 + SEPOS-043 — 76/76 tests passed, 3 manual checks pending. Saved to ~/Documents/Claude/Projects/SiamEpos/SiamEPOS-QA-Report-May2026.docx | Reporting |
| 2026-05-15 | Nook | Friday Night Stress Test v1 — 32/35 checks passed. 82 reservations, 277 covers, overbooking blocked, 5 bugs found for Krit. | SEPOS-STRESS |
| 2026-05-15 | Krit | BUG-001..005 — API hardening: 404 on add-items to ghost order, reject negative/zero payments, /api/tables name fallback, reject past reservation dates, 404 on PUT ghost reservation. Commit 937116d. | BUG-001..005 |
| 2026-05-15 | Nook | Friday Night Stress Test v2 — 35/37 passed. BUG-002,003,004,005 confirmed fixed. BUG-006 found. | SEPOS-STRESS |
| 2026-05-15 | Nook | Friday Night Stress Test v3 — 36/37 passed. BUG-006 found (pay ghost order→500). | SEPOS-STRESS |
| 2026-05-15 | Nook + Claude | Friday Night Stress Test v4 — 37/37 CLEAN. All bugs resolved. 82 reservations, 293 covers, 18 overbooking blocks, 0 crashes, 46.3s. System ready for production Friday service. | SEPOS-STRESS |
| 2026-05-15 | Krit | why-siamepos.html + book-demo.html refreshed — new hero, pain section (6 cards), generic-vs-SiamEPOS comparison table, founder story. Demo page: what-you'll-see checklist, 4-item trust signals, 3-step "What happens next" section, button copy updated to "See SiamEPOS in Action →". | MARKETING |
| 2026-05-16 | Maya | Nav consistency fixed across all 5 pages (features, why, book-demo, website-design) — SVG logo on all, uniform links: Features/Pricing/Why Us/Website Design/Book a Demo. website-design.html: prices £299→£199, £499→£299, Premium £799 removed. Portfolio section replaced with live www.siamepos.net Baan Siam showcase card. | MARKETING |
| 2026-05-16 | Maya | features.html: "Six features" section background fixed (white text was invisible on cream bg — now solid navy). "14 UK allergens" stat replaced with "AI / Scan invoice → stock & cost done" on index.html. Baan Siam portfolio card made bolder. OpenTable pricing corrected across features.html + pricing.html: removed false "£30/cover" — replaced with real published UK rates (£299/month + £1–2 per seated cover). | MARKETING |
| 2026-05-16 | Maya | why-siamepos.html: Added competitor comparison rows (support hours, invoice scanning, cost vs sales, CRM, VAT reporting, built for Thai restaurants). Removed clock-in/out row — VDIT does have that feature, kept comparison honest. | MARKETING |
| 2026-05-17 | Maya | Created lite.html — SiamEPOS Lite landing page. Hero, 3 pricing cards (Booking £29, Ordering £39, Bundle £49), ROI calculator strip, Pro upgrade comparison (£89, widgets included). Added "Lite" nav link across all 6 pages. | SEPOS-LITE |
| 2026-05-18 | Maya | GA tracking (G-R0E872VB21) confirmed on all pages. Google Search Console verified via GA method. Sitemap submitted + lite.html added to sitemap.xml. | SEO |
| 2026-05-17 | Pose | SEPOS-LITE-001 Phase 3 COMPLETE — Lite server (Express, JWT auth, onboarding, Stripe billing, restaurant/settings/bookings/orders/stats/embed-codes endpoints) + Lite client (React/Vite — LoginPage, OnboardingPage 4-step wizard, DashboardPage, BookingsPage, OrdersPage, SettingsPage, EmbedCodesPage). Deployed: siamepos-lite.netlify.app (Netlify) + lite-api.siamepos.co.uk (Railway). DNS propagation pending for lite-api custom domain — use Generate Domain URL as temp workaround until DNS live. All env vars set on both platforms. | SEPOS-LITE-001 |
| 2026-05-16 | Maya | Delivery messaging added to index.html + features.html — takeaway cards updated to "Collection or Delivery", mention Uber Direct or own driver. No promises made about how it works, just that SiamEPOS supports both options. | MARKETING |
| 2026-05-16 | Nick | Strategy: Uber Direct vs Uber Eats analysis. Uber Direct = white-label courier, zero commission, £3–6/delivery flat fee. Researched driver reliability risk — suburban Friday night peak is worst case (drivers busy elsewhere). Decision: Phase 1 = collection only (reliable, most UK Thai takeaway is collection anyway). Phase 2 = Uber Direct/Stuart as client opt-in, not a core feature. SEPOS-DELIVERY-001 deprioritised until Stripe live + real-world testing. SEPOS-ONBOARD-001 + Uber Direct API application tickets added to backlog. | STRATEGY |
| 2026-05-16 | Sandy | Baan Siam demo site (siamepos.net) — mobile nav fixed across all 7 pages (broken @media block causing burger menu to not show). Siam Kitchen portfolio page mobile nav built from scratch. Plern portfolio page: real photos (6 food shots + logo), gallery section, real address/phone/email from business plan. Accidentally overwritten plern.html restored from git. | DEMO SITE |
| 2026-05-16 | Sandy | Takeaway widget mobile UX overhaul — sticky cart bar at bottom of menu so users always see total without scrolling, horizontal scroll category tabs (no wrapping), 40px touch targets, iOS zoom fix (font-size:16px on all inputs), large tap-friendly pickup time cards, named step indicators, backdrop tap to close. | SEPOS-034 |
| 2026-05-16 | Sandy | Takeaway widget delivery UX — moved Collection/Delivery toggle + postcode radius check to Step 1 (before menu). Delivery: enter postcode → Check → confirmed ✅ → browse menu → address on details screen. Collection: one tap → straight to menu. Removed schedule/ASAP picker — always ASAP, shown at review. Order page updated with Collection + Delivery cards. | SEPOS-DELIVERY-002 |
| 2026-05-19 | Sandy | Baan Siam demo site (client/MockUp Website/) widget URLs updated — all 7 HTML files previously pointed takeaway-widget.js + booking SW_SERVER/SERVER variables at restaurant-epos-production.up.railway.app; updated to baan-siam.siamepos.co.uk throughout (order.html, index.html, about.html, contact.html, menu.html, book.html, portfolio.html). Korakot to git push. | SEPOS-CLIENT-001 |
| 2026-05-17 | Sandy | siamepos.co.uk nav overhaul — lite.html nav replaced with standard SiamEPOS logo + correct link format; "Pricing" renamed SiamPRO, "Lite" renamed SiamLite across all 7 pages; SiamLite moved to after SiamPRO in nav order. Fixed double nav bug on lite.html (footer nav.footer-links caught by nav{position:fixed} — added CSS override). Fixed mobile burger invisible on 5 pages (display:none base rule was after media query, added !important). Fixed iOS zoom: form inputs 14px→16px on index.html + book-demo.html. Fixed pricing.html website design prices: Starter £299→£199, Full £499→£299, Premium £799 card removed. Korakot to git push. | MARKETING |
| 2026-05-16 | Krit | SEPOS-025/026 printer round 2 — printService.js: TCP job queue (1.5s gap between connections to fix WAVLINK buffer overlap/garbled header), CAN byte, buildFireNotice (TABLE X / FIRE MAINS call card, no item list), name_alt below EN name in bilingual kitchen tickets. server.js: /api/print/kitchen-fire endpoint. api.js: serverPrintFireNotice. KitchenTicket.js: printFireNoticeTicket export + buildFireNoticeHTML. OrderScreen.jsx: Fire button now prints fire notice (not full ticket); kitchen+bar prints chained sequentially in background (fixes simultaneous TCP drop — only drink was printing). Bar popup pre-open fixed (Chrome 1-popup-per-gesture limit). | SEPOS-025/026 |
| 2026-05-16 | Krit | SEPOS-DELIVERY-002 — takeaway customers now flow into the Customers CRM (merged into /api/customers by email — note: there is NO customers table, CRM is derived from reservations + now takeaway orders). Widget Step 3 gets a Collection/Delivery toggle + address fields + consent checkbox. Kitchen shows 🚗 Online Delivery vs 🥡 Online Order. 4 new orders columns. Commits 94b75c5 + ed1336a. Phase 2 (Uber dispatch) NOT done — separate ticket. | SEPOS-DELIVERY-002 |
| 2026-05-16 | Krit | SEPOS-DELIVERY-002 radius check — takeaway widget geocodes the customer's postcode (postcodes.io, free, no key) and checks straight-line distance against the restaurant's delivery radius. Delivery toggle only appears once the operator sets a postcode + radius in Admin → Settings → 🚗 Online Delivery. Address fields are revealed only when the postcode is confirmed in-area; out-of-area customers get a one-tap "Order for Collection instead". New /api/takeaway/delivery-check endpoint + delivery_enabled flag on /api/takeaway/settings. | SEPOS-DELIVERY-002 |
| 2026-05-16 | Krit | Split bill print buttons — 🖨️ Print button added alongside Cash + Card on both Split Equally and Split by Item screens. Print marks person as paid + prints their individual receipt (split equally: all items, their share of total; split by item: only their assigned items). BillScreen.jsx only. | SPLIT-PRINT |
| 2026-05-16 | Krit | SEPOS-025/026 — Server-side ESC/POS network printing. New printService.js sends raw ESC/POS to printer IP:9100 via TCP. Works from iPad/browser/Electron on same WiFi. Settings UI: Admin → Settings → 🌐 Network Printers (IP + port for receipt/kitchen/bar + kitchen copies). ReceiptPrinter.jsx + KitchenTicket.js try server-side first, fall back to Electron/browser. 4 new endpoints: /api/print/test|receipt|kitchen|bar. | SEPOS-025/026 |
| 2026-05-17 | Korakot + Claude | Stripe account created (SiamEPOS LTD sandbox). Webhook set up → restaurant-epos-production.up.railway.app/api/stripe/webhook, 9 events, signing secret in Railway as STRIPE_WEBHOOK_SECRET. API keys (pk_test + sk_test) added to Railway. Stripe Products to create still pending (STRIPE_PRICE_LITE_BOOKING/ORDERING/BUNDLE/PRO). lite-api.siamepos.co.uk domain added to Railway (SiamEPOS Lite service) — DNS records added to Namecheap (CNAME lite-api → 3pvss4b7.up.railway.app + TXT verify record). Pending propagation. | STRIPE + INFRA |
| 2026-05-18 | Pose + Korakot | SiamEPOS Lite full deployment session. Fixed seedEposLogin (X-Setup-Secret header missing — seeds were silently 403ing). Manually seeded Baan Siam owner login. Fixed Netlify SPA routing. Updated plan prices £39/£49/£69. Fixed embed codes field name mismatch. Verified plan gating end-to-end. Added break/2nd session to opening hours. Added SVG favicon (navy + gold S). Added Revenue history page (bar chart + daily table, 7/30/90 day picker). Fixed revenue SQL interval syntax. All Railway env vars confirmed. Everything deployed and live. Next session: test online ordering end-to-end on the Lite dashboard. | SEPOS-LITE |
| 2026-05-18 | Korakot + Claude | Deliveroo Developer Portal set up. Partner Platform Suite sandbox active (Menu API, Orders API, Update Order Status API all GO). Credentials in Railway (DELIVEROO_CLIENT_ID, DELIVEROO_CLIENT_SECRET, DELIVEROO_ENV=sandbox). Production contract under review. SEPOS-DELIVEROO-001 ticket written for Krit. | SEPOS-DELIVEROO-001 |
| 2026-05-18 | Krit | SEPOS-DELIVERY-001 ✅ — Stuart + Uber Direct courier dispatch shipped (eec2449). stuartService.js + uberDirectService.js, 5 new DB columns, POST /api/delivery/dispatch|quote|stuart-webhook|uber-webhook. Admin 🛵 Courier Dispatch card. Kitchen: blue 🚗 Dispatch button → live courier status + 📍 tracking link. Delivery orders stay open until courier webhook reports delivered. Uber Direct dormant until credentials arrive. Verified live 2026-05-18 — /api/delivery/dispatch + /stuart-webhook responding, courier columns migrated; `item_name` dispatch-query bug fixed (342ea05) + redeployed. Full dispatch with a real delivery order + Stuart sandbox still needs a QA pass. | SEPOS-DELIVERY-001 |
| 2026-05-18 | Korakot + Claude | Stuart + Uber Direct setup: credentials in Railway, Stuart webhook live (all 5 topics, dashboard.stuart.com). Uber Direct applied — awaiting review. Deliveroo: Developer Portal live, sandbox active, credentials in Railway. | DELIVERY SETUP |
| 2026-05-18 | Korakot + Claude | Stripe testing complete. Webhook trigger tested via Stripe Workbench Shell — 200 confirmed, "no matching restaurant" is expected for test fixtures. Stripe webhook setup guide saved to memory for future reference. | SEPOS-STRIPE-001 |
| 2026-05-18 | Krit + Korakot | SEPOS-STRIPE-001 ✅ — Stripe webhook handler live. POST /api/stripe/webhook on restaurant-epos-production verifies signatures + handles customer.subscription.created/updated/deleted + invoice.payment_failed. Tested: trigger fires → 200 + "no matching restaurant" (expected for test fixtures — will match real restaurants when Stripe customer ID is stored on signup). STRIPE_WEBHOOK_SECRET confirmed in Railway env vars. | SEPOS-STRIPE-001 |
| 2026-05-18 | Korakot + Claude | SEPOS-LITE-002 ✅ CLOSED. Architecture: Model A ("Lite as Pro") confirmed — each Lite customer gets own Railway service + own Postgres DB. Phase 2b cancelled. Pricing locked: Booking £39/mo, Ordering £49/mo, Bundle £69/mo, Pro £89/mo. Stripe Products created for all 4 plans — price IDs added to Railway. Running cost docs filed: SEPOS-LITE-002-running-costs.csv + SEPOS-LITE-002-architecture-note.md in restaurant-epos/. | SEPOS-LITE-002 |
| 2026-05-17 | Korakot + Claude | SiamEPOS Lite product defined. Pricing: Booking widget £29/month, Ordering widget £39/month, Bundle £49/month, Full Pro £89/month. Hosting decision: same Railway server as Pro, plan flag gates features. Dashboard: existing ops.siamepos.co.uk with EPOS tabs hidden for lite plans. SEPOS-LITE-001 Phase 2a approved (tenant middleware, safe). Phase 2b (endpoint retrofit) held until first real Lite client. Nick briefed to write pitch doc. Maya briefed to build pricing page. Krit has SEPOS-LITE-001 ticket. | SEPOS-LITE-001 |
| 2026-05-17 | Korakot + Claude | web-design skill created for Sandy — covers premium restaurant design patterns, conversion principles, current 2025/2026 trends, practical CSS/JS implementation, SiamEPOS brand specifics, and structured critique format. Packaged as web-design.skill in SiamEpos folder. | SANDY SKILL |
| 2026-05-17 | Korakot + Claude | VDIT Solutions (vdit.co.uk) researched as main competitor. Maya briefed with verified facts. Comparison added to why-siamepos.html. Recommended cnfujun 80mm thermal printer to team (under £30, ESC/POS, USB+Ethernet, Mac confirmed working). | MARKETING |
| 2026-05-17 | Krit | SEPOS-LITE-001 Phase 1 — Multi-tenant schema foundation. restaurants registry table (restaurant_id, name, plan, api_key, status, Stripe IDs) seeded with default 'siamepos'. restaurant_id column added to all 19 tenant-scoped tables (orders, order_items, payments, menu_items, categories, staff, settings, etc.) — default 'siamepos'. Indexes on orders/order_items/menu_items. Design doc committed: SEPOS-LITE-001-design.md. Safe for Pro — purely additive, existing installs unaffected. tenantQuery helper + MULTI_TENANT flag deferred to Phase 2. Phase 2–4 gated on Korakot's answers to §14 open questions (hosting, dashboard location, plan tiers, billing owner). | SEPOS-LITE-001 |
| 2026-05-19 | Korakot + Claude | **SEPOS-CLIENT-001 ✅ — Baan Siam fully live as first real client (Model A).** URLs: **App** `app.baan-siam.siamepos.co.uk` (Netlify, `siamepos-baan-siam` project) → **API** `baan-siam.siamepos.co.uk` (Railway, `siamepos-baan-siam` service, own Postgres DB). Owner login: kongponsrisiri@gmail.com. Plan: pro. BREVO_API_KEY set. **Frontend:** Added `VITE_API_URL` support to `client/src/api.js` so each client's Netlify deploy can point to their own backend. Set `VITE_API_URL=https://baan-siam.siamepos.co.uk` in Netlify env vars. **Bugs fixed during provisioning (affects ALL fresh DB deployments):** (1) `database.js` — `ALTER TABLE orders ADD COLUMN staff_id REFERENCES staff(id)` ran before `staff` CREATE TABLE → moved after. (2) `database.js` — `ALTER TABLE reservations ADD COLUMN marketing_consent/unsubscribed_at/table_ids` ran before reservations CREATE TABLE → moved after. (3) `server.js /api/setup/restaurant` — used non-existent `id` + `email` columns on restaurants table (PK is `restaurant_id` VARCHAR) → fixed. **Full provisioning checklist for future clients:** (1) Run `./scripts/provision-client.sh` (2) Manually create Railway service + Postgres in dashboard (3) Press Enter in script → it sets env vars + deploys (4) Wait for green deploy, then run 2 curl commands to seed owner login + restaurants row (5) In Namecheap add CNAME `{slug}` → Railway URL + TXT verify record (6) In Railway add custom domain `{slug}.siamepos.co.uk` (7) Add BREVO_API_KEY to Railway env vars (8) In Netlify create new site from same repo, set `VITE_API_URL=https://{slug}.siamepos.co.uk`, deploy (9) In Namecheap add CNAME `app.{slug}` → `siamepos-{slug}.netlify.app` (10) In Netlify add custom domain `app.{slug}.siamepos.co.uk`. Total ~20 min. **Known limitation:** provisioning script seeds login/restaurant row immediately after deploy trigger but service may still be booting → re-run curls manually if warnings appear. | SEPOS-CLIENT-001 |
| 2026-05-21 | Sandy (Cowork) | **SPA-UI: Mobile UX + brand CI complete (SPA-UI-003).** App.jsx fully rewritten with bottom navigation bar (mobile ≤768px): Appointments / Clients / Admin tabs with SVG icons, gold active state, iPhone safe-area padding (`env(safe-area-inset-bottom)`). Desktop nav links + staff name hidden on mobile via `.desktop-only`. `BottomNav` component + `CalendarIcon`/`PersonIcon`/`GearIcon` SVG icons. AppShell: `100dvh`, 16px padding. `styles.css`: `.bottom-nav`, `.bottom-nav-item`, `.desktop-only`, `.app-main` CSS rules. All pending changes committed (SPA-UI-002 + SPA-UI-003). Korakot to `git push` from Terminal. | SPA-UI-003 |
| 2026-05-21 | Sandy (Cowork) | **SiamSpa UI brand overhaul — screens aligned to SiamEPOS CI (SPA-UI-002).** Login + TopNav were already branded from SPA-UI-001. This pass covered: **(1) styles.css** — added `button.gold` class (gold bg, navy text — used for voucher/checkout CTAs), `button.outline`, global `thead th` style (light bg, uppercase muted labels, 2px bottom border), `tbody td` hover state, `.stat-card` class (navy top-border accent, bold navy value), `.section-header` class (flex header with title + subtitle + optional right-side action, 2px bottom divider), `.tab-strip` class. **(2) TradingSection** — stat cards now use `.stat-card` with bold navy numbers; date filter moved into `section-header` right slot; "Trading" `h2` + subtitle added. **(3) Every admin section** — consistent `section-header` with `h2` + subtitle added to all 13 sections: Trading, Reports, ZReport, Bills, Clients, Campaigns, Vouchers, Treatments, Therapists, Rota, Rooms, Booking, Embed Codes, Settings. **(4) NewAppointmentModal** — client-selected panel changed from `#f0f9ff`/`#bae6fd` (Bootstrap blue) to `var(--gold-light)` / gold border. Client search dropdown hover changed to `var(--navy-muted)`. **(5) CheckoutScreen** — voucher toggle button + redeem button now use `className="gold"` instead of inline hex values. All div counts verified — mismatches were self-closing `<div/>` false positives, all files structurally correct. | SPA-UI-002 |
| 2026-05-20 | Krit (Cowork) | **Reverted Sandy's commit 74e4ad4** — removed add/delete category UI from MenuSection.jsx, removed `addCategory`/`deleteCategory` from api.js, removed POST/DELETE `/api/categories` endpoints from server.js. Sandy's changes were breaking app.siamepos.co.uk. Korakot to `git push` from terminal to deploy fix. | HOTFIX |
| 2026-05-19 | Krit | Reservation floor plan — table panel sort order fixed. Was sorting by capacity first (2p→3p→4p) which scattered T5, T6, T21 etc to the bottom. Now sorts by table_number only so panel reads T1, T2, T3… in sequence. One-line fix in ReservationPlanView.jsx. | RESERVATIONS-UX |
| 2026-05-18 | Krit | Kitchen screen card density + delivery address UX — added KitchenCardHeading (order label on one line, nowrap+ellipsis); delivery address pulled into its own blue strip below the card header (always fully visible, never truncated — in-house driver can read it straight off the screen); fixed double-padding bug (was 24px wasted per card); 4-column fixed grid (repeat(4,1fr)); header padding trimmed; Pass tab gets same heading treatment. | KITCHEN-UX |
| 2026-05-16 | Krit | SEPOS-025/026 — Mac desktop-app silent printing. Electron `list-printers` + `print-html` IPC (webContents.print, no dialog); Admin → Settings → 🖨️ Printer card picks receipt + kitchen printers per-device (localStorage) with a Test print button. Receipts print silently to the chosen printer, browser print-dialog kept as fallback. Kitchen tickets auto-print an 80mm ticket when a course is fired (KitchenTicket.js). Works with any OS-installed thermal printer incl. cnfujun 80.E-Z04-AA. iPad/browser keeps the print dialog. | SEPOS-025/026 |
| 2026-05-16 | Krit | Receipt printing fix — diagnosed "prints raw code on paper" on the cnfujun POS80: the Mac printer queue was on the **Generic PostScript** driver. Switched the CUPS queue to the **POS-80** thermal driver (`lpadmin`), re-enabled + cleared the stuck queue. Browser receipt printing confirmed working. Printer gotcha documented in Announcements. | SEPOS-025 |
| 2026-05-16 | Krit | Cut desktop release **v1.5.2** — version bumped, tag pushed, GitHub Actions Release build running. Carries SEPOS-025/026 (printer), SEPOS-043, SEPOS-046, BUG-001..005 and SEPOS-DELIVERY-002 to the Mac + Windows desktop apps. | Release v1.5.2 |

---

## 📢 Announcements

| Date | From | Message |
|------|------|---------|
| 2026-05-20 | Sam | **SiamSpa Railway env vars — action needed after Railway recovers.** (1) Set `UNSUB_SECRET` on spa-api service — run `openssl rand -hex 32`. If unset, insecure default works but tokens never expire. (2) Set `SPA_ADDRESS` for GDPR email footer (e.g. "47 Charlotte Street, London W1T 4ED"). (3) Confirm `BREVO_API_KEY` is set — required for booking confirmation emails + campaigns. Note: `clients.unsubscribed_at` + `campaigns` table migrate automatically on Railway boot. |
| 2026-05-19 | Korakot | **⚠️ ONBOARDING NOTE — Always recommend the desktop app to new clients.** On 19 May 2026, Railway AND Netlify both had simultaneous outages (Railway: "unconditional drop overload"; Netlify: "elevated response times affecting origin services"). Any client on cloud-only mode (browser/iPad) would have been completely offline during this. The Electron desktop app on Mac avoids this — it runs on local SQLite and queues changes until the cloud comes back (SEPOS-PRO-001 offline sync). **Action for every new client signup:** (1) Strongly recommend installing the Mac desktop app as the primary till — not just the browser. (2) Explain that browser/iPad tabs are great as secondary screens but should not be the only terminal. (3) Cloud outages are rare (Railway/Netlify both 99.9%+ uptime) but do happen — having the desktop app means the restaurant never stops taking orders. |
| 2026-05-19 | Korakot | **SiamSpa competitive intel — VDIT actively targeting Thai massage shops NOW.** VDIT sales reps are cold-calling Thai massage/spa shops pitching online booking. Their key selling points: (1) Online booking widget — customer picks date, time, service. (2) **Therapist/staff selection at booking time** — customer picks their preferred masseuse directly. (3) Reduces LINE/WhatsApp chat overload. (4) Prevents double-booking. **What this means for SPA-001 (Sam):** Therapist selection at booking time is a must-have feature — VDIT is already closing deals on this. SiamSpa's advantage is giving the full picture: bookings + staff clock-in + revenue + CRM in one place vs VDIT's booking-only. Duration-aware slots (60min/90min blocks a room for the full duration) and room/bed management (rooms instead of tables) are the key spa-specific features to prioritise. **The market is clearly ready to buy — speed matters.** Every week VDIT is out there locking shops into contracts. @Sam — factor therapist selection into SPA-001 scope as P1, not P2. |

| Date | From | Message |
|------|------|---------|
| 2026-05-18 | Korakot | **Thank you all.** Krit, Pose, Nook, Sandy, Maya, Nick, Sam, Kai, Nong — what we've shipped in the last two weeks is remarkable. Full EPOS, delivery, printing, Lite product, marketing site, demo site, stress tests, QA, strategy — all of it. I genuinely couldn't do this without every one of you. Keep going, we're building something real. 🙏 |
| 2026-05-16 | Sandy (Korakot directive) | **MOBILE FIRST — new rule for all web work.** Every website, widget, or page must be designed and tested on mobile before desktop. Min 44px tap targets, 16px+ form inputs, hamburger nav, no horizontal scroll, key actions above the fold on phone. Full checklist in CLAUDE-Sandy.md. This applies to: siamepos.co.uk, siamepos.net, booking widget, takeaway widget, and any future client sites. |
| 2026-05-16 | Korakot | All | **RECOMMENDED RECEIPT PRINTER for clients:** cnfujun 80mm thermal printer — under £30, ESC/POS (Epson compatible), USB + Ethernet built in, works on Mac + Windows + Linux. Model 80.E-Z04-AA. Driver download: cnfujun.com/d/33 (direct download, no page shown — just paste URL in browser). Korakot tested and confirmed working on Mac. Recommend this to all new SiamEPOS clients as the budget receipt printer option. |
| 2026-05-16 | Krit | All (esp. Nong / onboarding) | **⚠️ PRINTER SETUP GOTCHA — cnfujun / POS80 thermal printer.** Symptom: receipts print as raw HTML/code on paper while the on-screen preview looks fine. Cause: macOS auto-installs the printer with the **"Generic PostScript Printer"** driver — the thermal printer can't read PostScript so it prints the page code as text. FIX: install the cnfujun driver (cnfujun.com/d/33), then in System Settings → Printers & Scanners set the printer's driver/"Use" to the **POS-80** driver (NOT Generic PostScript, NOT Generic PCL). Verify with Terminal: `lpoptions -p <printer>` should show `printer-make-and-model=POS-80`. Every client installing this printer will hit this — make it a step in the onboarding checklist. |

---

## 📋 Handoffs & Notes

*Use this section to leave messages for other agents. Delete entries once actioned.*

| Date | From | To | Message |
|------|------|----|---------|
| 2026-05-23 | Krit (Cowork) | **BO-SPA-001 — ops.siamepos.co.uk multi-product support (Phase 1 UI).** Added `product VARCHAR(20) DEFAULT 'restaurant'` to `clients` table (schema.sql migration + backend route). Back-office now understands `restaurant` vs `spa` clients throughout: **(1) DB/backend** — `product` field on `POST /api/clients`, `POST /api/clients/onboard`, `PUT /api/clients/:id` whitelist. **(2) theme.js** — `PLAN_LABEL.spa = 'Spa £49/mo'`; new `PRODUCT_BADGE` export (`🍽 Restaurant / 🌿 Spa`). **(3) DashboardPage** — product filter chips (All · 🍽 Restaurant · 🌿 Spa) above client list; 🌿/🍽 badge on every client card; Quick Add modal has product picker + plan auto-swaps. **(4) NewClientWizard** — new Step 0 "Choose product" (two large cards); selecting Spa auto-sets plan=spa, fee=£49, features card shows spa capabilities instead of takeaway/inventory checkboxes; Step 1 relabels "Restaurant name" → "Spa name"; Step 5 review shows Product row. **(5) ClientDetailPage** — product badge in hero, Plan dropdown shows spa option only for spa clients, Website Builder tab hidden for spa clients. **Pre-req for Pose (BO-SPA-001 Phase 2):** all the wizard + detail page scaffold is ready — Pose now just needs to add provision-client.sh spa branch + Stripe spa price IDs (after Korakot creates spa Stripe Products). | BO-SPA-001 |
| 2026-05-23 | Krit | Pose | **BO-SPA-001 — Back-office multi-product support (Korakot decision: ONE shared back office for restaurant + spa).** Rationale: back office is meta-management (Railway provisioning, Postgres, Stripe subscriptions, health, support, finance) — ~95% identical for either product. Forking would double maintenance + split MRR/churn view forever for tiny UI saving. Decision: keep ONE back office, gate the product-specific bits on a `product` field. **Scope for you to build:** (1) **Schema** — add `product VARCHAR(20) DEFAULT 'restaurant'` to the back-office `clients` table; migration sets existing rows to `'restaurant'`. (2) **NewClientWizard** — new first step "Choose product" (Restaurant / Spa cards). Plan picker loads plans for the chosen product: Restaurant = `lite_booking £39 / lite_ordering £49 / lite_bundle £69 / pro £89` (existing); Spa = TBD with Korakot. (3) **`scripts/provision-client.sh`** — add `--product=<restaurant|spa>` flag (or interactive prompt as step 1). Spa branch clones from the `spa-epos/` codebase, sets `SPA_NAME` / `SPA_EMAIL` etc. instead of `RESTAURANT_*`, and post-deploy curls `/api/spa` (or equivalent) instead of `/api/restaurant`. Keep the existing env-var-verification pattern (`4f13cd9`) — same idea, just `SPA_ID` instead of `RESTAURANT_ID`. (4) **Stripe price IDs** — Korakot creates spa Stripe Products; add `STRIPE_PRICE_SPA_*` env vars on the back-office Railway service. The Stripe webhook handler already picks plan from price ID — just extend the map. (5) **Client list UI** — small product badge per row (🍽 Restaurant / 🌿 Spa). Optional filter chip to show one product at a time. (6) **Conditional quick-actions** — "View menu" restaurant-only; "View treatments" spa-only. Health / billing / Refund / Stripe-dashboard link / re-seed creds stay shared (product-agnostic). (7) **Trading / Finance reports** — verify they don't choke when clients span products. Optional: a "by product" group-by in the P&L (low priority). **Out of scope (defer to v2):** clients buying BOTH products, migrating any existing spa demo deployments into the registry. **Pre-reqs from Korakot before you start:** (a) spa plan tiers + prices, (b) spa Stripe Products created so `STRIPE_PRICE_SPA_*` can be set, (c) domain pattern decision — keep `<slug>.siamepos.co.uk` for spa clients too OR use `<slug>.siamspa.co.uk` (separate brand domain — branding call). Sync with Krit if the back-office ↔ client-deployment contract (env var names, `/api/restaurant` ↔ `/api/spa`, etc.) needs adjusting on the client side. |
| 2026-05-22 | Nook | Krit | **BUG-EPOS-001 — `GET /api/reservations/settings` returns HTTP 500.** Root cause: the handler at `src/server.js` ~line 1795 queries deprecated column names from before the SEPOS-049 schema migration (`slot_interval`, `default_duration`, `service_start`, `service_end`, `use_split_service`, `confirmation_email`). None of these exist in the current `restaurant_settings` table. Fix: copy the SELECT from the working `GET /api/reservations/settings/:restaurantId` handler at ~line 1815, which already uses the correct column names (`service_type`, `lunch_service_start`, `lunch_service_end`, `dinner_service_start`, `dinner_service_end`, `slot_interval_mins`, `booking_lead_hours`, `booking_advance_days`). One-line diff. | EPOS-SMOKE |
| 2026-05-23 | Korakot | Sam | **SPA-ONBOARD-001 — SiamSpa client onboarding (Model A).** Korakot decision 2026-05-23: each SiamSpa client (including trials) gets their own Railway service + own Postgres DB from day 1. No shared multi-tenant server — SiamSpa has no `spa_id` columns so a shared server would mix all spas' data. **What to build:** **(1) Email login on the spa app.** Currently spa app is PIN-only. Add "Sign in with email →" link at the bottom of the login screen — opens email + password form. JWT stored in localStorage, 14-day expiry. Reuse `POST /api/auth/set-credentials` pattern from SEPOS-LITE-003 (body: `{ email, password, name }`, header `X-Setup-Secret`). **(2) VITE_API_URL support on spa frontend.** Spa frontend must read `import.meta.env.VITE_API_URL` as the backend URL (fallback to `https://spa-api.siamepos.co.uk`). Mirror the pattern in `client/src/api.js`. Each client's Netlify deploy sets `VITE_API_URL=https://spa-api.{slug}.siamepos.co.uk`. **(3) `provision-spa.sh` script.** Clone of `scripts/provision-client.sh` for the spa-epos repo: prompt slug + spa name + owner email + password → Railway service → set env vars → deploy → seed owner login via set-credentials → verify with `/api/health`. **(4) DNS + Netlify per client.** Namecheap CNAME `spa-api.{slug}` → Railway, custom domain on Railway, new Netlify site with `VITE_API_URL`, Namecheap CNAME `spa-app.{slug}` → Netlify. **Pricing:** £49/month per spa. Railway ~£5–10/month per client. **Reference:** SEPOS-LITE-003, SEPOS-CLIENT-001, provision-client.sh. Full spec: `~/Documents/Claude/Projects/SiamEpos/SPA-ONBOARD-001-Sam-Ticket.docx` | SPA-ONBOARD-001 |
| 2026-05-22 | Nook | Korakot | **⚠️ SECURITY — PIN "0000" accepted on production.** During the smoke test, `POST /api/staff/login` with PIN "0000" returned HTTP 200 (valid session). A staff member in the production DB has a trivially guessable PIN. Please go to Admin → Staff and change any PINs that are "0000", "1234", or similarly weak. | EPOS-SMOKE |
| 2026-05-22 | Nook | Korakot | **Cleanup — test order #243 open in production.** A test order was created during the smoke test (POST /api/orders) and left open because the order ID wasn't captured on the first run. Please delete it via Admin → Bills or `DELETE /api/orders/243` with a manager PIN. | EPOS-SMOKE |
| 2026-05-22 | Nook | Korakot | **SPA-PAY-001 — 7 manual checks to verify once Stripe env vars set on Railway.** (1) Open the booking widget → complete a real Stripe card payment → confirm appointment created with deposit_amount set and payment_status=deposit_paid. (2) Check confirmation email: "Manage your booking" gold CTA opens `spa-api.siamepos.co.uk/my-booking.html?token=…` correctly. (3) Click the manage link → reschedule to a different time → reschedule email arrives + appointment updated in admin. (4) Cancel inside the cancel window (> 24h before appointment) → Stripe refund issued → payment_status=refunded → cancellation email states the refund amount. (5) Cancel outside the cancel window → no refund → email states deposit forfeit per policy. (6) Admin → 🌐 Online Booking tab: stats strip shows upcoming count + deposits held total; bookings list shows the Stripe-paid booking with a link to the Stripe dashboard. (7) Click Refund button on a booking → payment_status flips to refunded in both the admin list and the appointment record. **Required Railway env vars before testing:** STRIPE_PUBLISHABLE_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, BOOKING_SECRET (openssl rand -hex 32). | SPA-PAY-001 |
| 2026-05-21 | Sam | Korakot | **SPA-003 — production checklist after push.** (1) Set `TREATWELL_WEBHOOK_SECRET` in Railway env (any long random string — give the same value to Treatwell when you register the webhook). (2) In the Treatwell partner portal, register the webhook URL `https://spa-api.siamepos.co.uk/api/treatwell/webhook` and add the secret as the `X-Treatwell-Secret` header. (3) When you get a real Treatwell test booking, check Admin → Clients — the client should appear with a 🌐 Treatwell pill in the Source column. (4) Check Admin → Reports — today's date should show a `🌐 Treatwell` tile in the Booking source card. (5) Open the Treatwell appointment and check out — the payment grid should have a 🌐 Treatwell button alongside Cash/Card/Split/Voucher. (6) If Treatwell's actual payload differs from what I assumed (booking_id / status / scheduled_for / customer / service), the field mapping is in `src/routes/treatwell.js` — easy adjustment. |
| 2026-05-21 | Sam | Nook | **QA SPA-003 + SPA-CRM-001 after Korakot pushes + sets env vars.** **Admin Clients tab:** (1) Admin → 👤 Clients should show status pills (VIP/Regular/New/Lapsed), a source filter dropdown, all live clients with their stats. (2) Tap a status pill — list filters to that segment. (3) Tap a source filter — Treatwell isolates marketplace clients, "Direct" isolates everyone else. (4) Marketing-consent toggle on/off, refreshes inline. (5) Export CSV — file includes Source column. (6) Click a row → opens existing ClientProfileScreen unchanged. **Top-level Clients screen:** list cards now show status pill + visits + lifetime spend + last-visit string. **Treatwell webhook:** (7) `curl -X POST $API/api/treatwell/webhook -H 'x-treatwell-secret: $SECRET' -H 'content-type: application/json' -d '{ booking_id, status:"confirmed", scheduled_for, customer:{name,phone,email}, service:{name,duration_minutes,price} }'` → `201 created`. (8) Re-send same booking_id → `200 duplicate` (no second appointment). (9) Send `status:"cancelled"` for that booking_id → `200 cancelled` (appointment flipped). (10) Without the secret header → `401`. **Checkout:** (11) Book a Treatwell appointment manually (via NewAppointmentModal — set source='treatwell' if exposed, else use a webhook), open Checkout, confirm the 🌐 Treatwell button appears alongside the others. (12) Tap it → bill closes with payment_method='treatwell'. **Reports:** (13) Admin → Reports — "Booking source — {date}" card shows one tile per source on the To date. Pre-existing PG syntax bug in `/reports/trading` was patched as part of this — confirm Reports loads without 500. | SPA-003, SPA-CRM-001 |
| 2026-05-21 | Sam | Korakot | **SPA-DEMO-001 ready for push + Netlify setup.** Files live at `spa-epos/mockup-website/` (5 pages + assets + netlify.toml + README + seed script). To go live: (1) `git push` so spa-api gets the new `photo_url` migration on Railway boot. (2) `node spa-epos/scripts/seed-demo.js --api=https://spa-api.siamepos.co.uk` to populate the production DB with the demo content. (3) In Admin → Therapists, deactivate any non-Baan-Siam-Spa therapists left over from earlier testing (Admin, Fah, Kot, Nan locally — check what's on prod). (4) New Netlify site → import restaurant-epos repo → Base directory `spa-epos/mockup-website` → Publish `.` → no build command. (5) Netlify → add custom domain `www.siamepos.com` + apex. (6) Namecheap → CNAME `www` → Netlify URL + ALIAS apex. (7) Verify https://www.siamepos.com loads the lotus hero, treatments page renders live menu, therapists page shows 4 cards with photos and "Book with X →" deep-links into the widget step 3. |
| 2026-05-21 | Sam | Nook | **QA SPA-DEMO-001 — Baan Siam Spa demo site at www.siamepos.com.** After Korakot pushes + sets up the Netlify deploy + seeds production: (1) Open www.siamepos.com on a phone first — hero loads with lotus, "Book your treatment →" button visible above the fold, burger nav opens cleanly. (2) Tap Treatments — should show 5–6 category sections, each with multiple treatments, prices in £, "Book this →" buttons. Tap a "Book this →" — widget should open AT STEP 2 (therapist picker) with the treatment already chosen (notice step indicator: dot 1 + 2 lit). (3) Tap Therapists — should show 4 profile cards (Anong / Malee / Niran / Suthida) with photos, specialisms, "Book with {Name} →" button. Tap "Book with Anong →" — widget should open AT STEP 3 (date+time) with both treatment AND therapist pre-set. (4) Date+time grid should render only Anong's slots. Pick one, fill details, confirm. (5) Brevo confirmation email arrives if email supplied. (6) In the SiamSpa admin → Trading or Appointments, the booking should appear assigned to Anong. (7) Test mobile burger nav across all 5 pages. (8) Test "Book now" CTAs in nav + footer + about page CTA — all open the widget. (9) Edge: try `?api=` URL override and confirm widget calls the right backend. Demo content seeded by `scripts/seed-demo.js`. | SPA-DEMO-001 |
| 2026-05-19 | Sam | Nook | **BUG-SPA-001 patched + re-tested locally → 55/55 green.** `src/routes/clients.js` PUT /:id/medical INSERT path now COALESCEs NOT NULL columns (booleans → FALSE, blood_pressure → 'none'). Once Korakot pushes, please re-run `node test-spa.js` against the live spa-api to confirm parity. |
| 2026-05-19 | Sam | Nook | **QA SPA-002 — public booking widget + therapist selection.** After Korakot pushes + Railway boots (which migrates `therapists.specialisms`), test on `spa.siamepos.co.uk`: (1) Admin → Therapists → edit one therapist → set Specialisms to e.g. "Deep tissue, Hot stone" → Save. The Specialisms column should now show that text in the row. (2) Admin → Embed Codes → tweak the API origin box if needed → tap **Copy** on Option 1 → paste into any plain HTML file → open it in a browser. The page should auto-render a "Book your treatment" button (no `<script>` errors in console). (3) Tap the button → step 1 lists treatments grouped by category → pick one → Continue. (4) Step 2 lists "Any available therapist" first (with ✦ icon) then every active therapist with their initials avatar + specialisms text. Pick a specific therapist → Continue. (5) Step 3 shows "Showing times for {therapist}." plus a date picker + time grid filtered to that therapist's free slots only. (6) Pick a time → Continue → step 4 name+phone+(optional)email+notes+two consent checkboxes → Confirm. (7) Step 5 confirmation card shows When / Therapist / Room / Reference #. Brevo confirmation email lands if email was supplied. (8) Log into the spa frontend and open AppointmentScreen — the booking should appear on the right therapist/room. (9) Repeat with **Any available** in step 2 — backend should auto-pick a free therapist. (10) Try to book the same slot twice from two browser tabs — second attempt should get "slot no longer available" (409). Edge cases: leave email blank (should still book), uncheck GDPR (Confirm button stays disabled), pick a far-future date (should still find slots within opening hours). | SPA-002 |
| 2026-05-19 | Krit | Nook | **QA reservations multi-table join + floor-plan date.** (1) Reservations → floor plan or timeline → open a booking → in the panel tap several tables: they join, the "seats X of Y" summary turns green once covered; the booking should then show across ALL those tables on both timeline + floor plan. (2) A 🔗 linked combo button selects the whole group in one tap; "Clear all tables" empties it. (3) Floor plan now has a date picker (◀ date ▶ Today) like the timeline — step to a future date, tap an empty table: the walk-in button should be GONE (replaced by a "today only" note) but you can still pre-assign that date's bookings to the table. (4) Back on today → walk-in + seat-now unchanged. (5) Existing single-table bookings still display + behave normally (table_ids falls back to table_id). Commits ab492f6 + 8e06d56. |
| 2026-05-18 | Krit | Nook | **QA SEPOS-DELIVERY-001 — courier dispatch (Stuart).** Setup: Admin → Settings → 🛵 Courier Dispatch — enable it, provider Stuart, set pickup address + phone, Save. Tests: (1) Place a **delivery** takeaway order via the widget. (2) In the Kitchen the delivery card shows a blue **🚗 Dispatch Courier** button — tap it, confirm → the card should switch to courier name + live status ("finding rider") + a 📍 tracking link. (3) Cook + serve all items — the delivery order must **stay open**; it only closes when the courier `delivered` webhook fires (collection orders still auto-close as before). (4) Collection takeaway orders unchanged — still 🥡 Collected + auto-close. (5) After a failed/cancelled dispatch the button returns as **🔁 Retry Courier** and re-dispatch works. (6) Selecting Uber Direct (no creds yet) should give a clean "not configured" error, not a crash. Stuart runs in sandbox (`STUART_ENV=sandbox`) so test jobs are free. Korakot + Claude already confirmed one dispatch end-to-end — this is the full QA pass. |
| 2026-05-18 | Krit | Nook | QA SEPOS-049 + SEPOS-050. **SEPOS-049 (Reservations → Timeline view):** with linked tables configured (e.g. four 2-tops linked), assign a party of 2 to ONE linked table — only that table's row should fill, the sibling rows stay free; assign a party of 8 — it should appear across all linked rows (dashed "ghost" blocks on the siblings). Floor Plan view + EPOS map were already correct (SEPOS-044); only the Timeline was broken. **SEPOS-050:** Admin → Reservation Settings now has "Maximum party size for online booking" (default 8) + "Restaurant Phone". Booking widget guest stepper stops at the cap and shows "call us on <phone>"; a covers>cap widget booking is rejected server-side; staff-created bookings are NOT capped. Smoke-tested live on Railway (party of 9 rejected, 8 OK) — needs a full QA pass. Possible follow-up to log: online availability checks the per-slot covers budget only, not whether a table/linked group actually fits the party — Korakot is aware, separate ticket if wanted. |
| 2026-05-18 | Krit | Pose | ▶️ **ACTION — wire the set-credentials integration into the Lite onboarding (Korakot directive 2026-05-18).** SEPOS-LITE-003 (email login for Lite owners) is built + pushed (874ebc7). **What to do:** in your Lite onboarding/provisioning flow, after a Lite deployment is up, create the owner's login by calling **`POST /api/auth/set-credentials`** on that deployment — body `{ email, password, name }`, header `X-Setup-Secret` = the deployment's `AUTH_SECRET` env var. It upserts an `admin`-role row on the **`staff`** table with a `crypto.scrypt` password hash, and returns `{ id, created|updated }`. **Do NOT** write to `lite_users` for the main-app login — under Model A the credential lives on the deployment's own `staff` table (`lite_users` was the pre-Model-A design). After this, the owner taps "Sign in with email" on the normal login screen → 14-day token → SEPOS-LITE-002 plan gating applies. Test end-to-end: provision a test Lite deployment → set-credentials → confirm the owner can email-login and sees only their plan's features. Sync with Krit if the endpoint contract needs adjusting.
|------|------|----|---------|
| 2026-05-18 | Krit | Pose | ⚠️ **SEPOS-LITE DIRECTION CHANGED — re-align with Korakot before any more Lite work.** Korakot has confirmed **Model A ("Lite as Pro")** — each Lite customer gets their own backend + own database, exactly like a Pro client (full reasoning: SEPOS-LITE-002-architecture-note.md). Two consequences for your completed Phase 3: (1) Lite customers use the **FULL SiamEPOS app** (app.siamepos.co.uk) gated by plan — NOT a separate dashboard — so the separate Lite client you built + deployed (DashboardPage/BookingsPage/OrdersPage/SettingsPage at siamepos-lite.netlify.app) is **superseded; please stop polishing it**. (2) Lite customers are **managed from the existing back office (ops.siamepos.co.uk)** — they are just clients in the client list with `plan = lite_*`, reusing the provisioning/health/billing you already have for Pro. **Revised Phase 3 scope (back-office work — your area):** (a) `NewClientWizard` gets a plan picker — lite_booking/lite_ordering/lite_bundle/pro; (b) onboarding provisions a Lite deployment (own Railway service + Postgres) the same way Pro is provisioned (SEPOS-029); (c) Stripe **subscription** billing per plan, writing back to the client's `plan`. Krit owns the in-app plan-gating (SEPOS-LITE-002 matrix). Phase 2b is **cancelled** (Model A removes it). Please sync with Korakot on whether the customer-facing siamepos-lite.netlify.app signup page stays as self-serve or is replaced by back-office onboarding. |
| 2026-05-16 | Krit | Nook | Please create a printer setup manual for clients. Cover: (1) What hardware to buy — WAVLINK USB 2.0 Network Print Server + compatible thermal printer (Epson TM-T20III or Star TSP143III recommended). (2) Physical setup — plug USB printer into WAVLINK, plug WAVLINK into router via Ethernet. (3) Finding the printer IP — log into router at 192.168.1.1, find WAVLINK in device list, assign it a static/fixed IP so it never changes. (4) SiamEPOS setup — Admin → Settings → 🌐 Network Printers → enter IP + port 9100 for receipt/kitchen/bar → Test → Save. (5) Troubleshooting — printer not found: check same WiFi, check IP hasn't changed, reboot WAVLINK. (6) ⚠️ IMPORTANT — Silent auto-print vs popup: When accessing SiamEPOS via app.siamepos.co.uk (cloud), print jobs go through Railway server which cannot reach a local printer IP — so a browser print dialog popup will appear. To get fully silent automatic kitchen tickets with NO popup, the till Mac must access SiamEPOS via the LOCAL network URL instead. Go to Admin → Settings → Network Setup, copy the URL shown (e.g. http://192.168.1.x:3001) or scan the QR code, and bookmark that URL on the till Mac. When using the local URL, the server runs on the Mac and can reach the printer directly — no dialog, fully automatic. iPads on the same WiFi should also use this local URL for silent printing. Keep it simple with screenshots if possible. Both English and Thai versions please. |
| 2026-05-14 | Claude | Sam | Spa CLAUDE.md is saved at ~/Documents/Claude/Projects/SiamEpos/CLAUDE-Sam.md — copy to ~/Desktop/restaurant-epos/spa-epos/CLAUDE.md once you create that folder |
| 2026-05-15 | Nook | Krit | PERF-001 (LOW): Block 5B concurrent item-adds — 5/10 Railway requests timeout under burst load. Likely connection pool exhaustion. Not blocking production but worth increasing the DB pool size if it appears in real service. |
| 2026-05-16 | Nick | Krit | ACTION REQUIRED: Add ANTHROPIC_API_KEY to back-office Railway env vars — unlocks AI website import (scrape client URL → auto-populate name, tagline, address, about). This is already built in ops.siamepos.co.uk, just needs the key. |
| 2026-05-16 | Korakot | All | ✅ Anthropic API key rotated and updated. Restaurant EPOS Railway env + Finance settings page both updated. AI invoice scanner and AI summary should be working again. |
| 2026-05-16 | Nick | Nong | For client onboarding NOW: open a new Claude chat, paste NONG.md as context + client details (name, covers, tables, staff, menu categories, service charge). Claude walks through setup checklist in Thai + English. Use this until SEPOS-ONBOARD-001 is built. |
| 2026-05-16 | Nick | Korakot | ACTION: Apply for Uber Direct API access NOW at developer.uber.com/docs/deliveries/overview — approval takes 1–2 weeks. Start clock while Krit finishes Stripe (SEPOS-040). Uber Direct = white-label courier, zero commission, ~£3–6/delivery flat. Completely different from Uber Eats marketplace. |
| 2026-05-16 | Krit | Nook | SEPOS-DELIVERY-002 built + pushed (94b75c5). Ready to QA. Heads-up for the spec: there is NO `customers` table — the CRM is a derived view over `reservations`, so I extended /api/customers to merge takeaway orders by email instead of the customers/customer_id approach in the ticket. Same outcome. To test: place a takeaway order via the widget (try both Collection + Delivery), check it appears in Admin → Customers, and check the kitchen shows 🚗 for delivery. |
| 2026-05-16 | Krit | Nook | SEPOS-025/026 printer (kitchen+bar simultaneous) still not fully resolved. Sequential chaining pushed. Next session: if still only drink prints, try extending printService `setTimeout` gap from 1500ms → 3000ms, or test with a printer on Ethernet direct (not WAVLINK USB). |
| 2026-05-16 | Krit | Nook | DELIVERY radius check shipped — to QA: in Admin → Settings → 🚗 Online Delivery set a restaurant postcode + radius (try 3 miles), Save. On the takeaway widget Step 3 a Delivery toggle should appear; pick Delivery, type an in-area postcode → Check → address fields appear; type a far-away postcode → Check → "outside our delivery area" + collection fallback. With both Settings fields blank, the Delivery toggle should NOT appear at all. Distance is straight-line via postcodes.io. |
| 2026-05-16 | Krit | Korakot | **v1.5.2 cut — CI building.** Tag `v1.5.2` pushed; GitHub Actions Release workflow building the Mac DMG + Windows EXE (~15–20 min). When CI finishes, a `SiamEPOS v1.5.2` GitHub Release appears with the installers. NEXT (Korakot, back later): reinstall the v1.5.2 DMG on the Mac to get onto the build, then the desktop-app silent printing is testable. Carries SEPOS-025/026, 043, 046, BUG-001..005, DELIVERY-002. |
| 2026-05-16 | Krit | Nook | SEPOS-025/026 printer QA — v1.5.2 desktop build now cut & building. Once released + installed on the Mac, test: Admin → Settings → 🖨️ Printer → pick Receipt + Kitchen printer → "Test print" prints silently (no dialog); pay a bill → receipt prints; add items to an order + hit 🔥 Fire → 80mm kitchen ticket prints. In a plain browser the Printer card shows a "desktop app only" note and receipts use the print dialog (correct). IMPORTANT: the Mac printer must be on the **POS-80** driver, not Generic PostScript — see the printer gotcha in Announcements. |
| 2026-05-16 | Claude | Maya | ✅ DONE — VDIT comparison added to why-siamepos.html. |
| 2026-05-17 | Claude | Nick | ACTION: Write a pitch document for SiamEPOS Lite (widgets-only product for restaurants already on another EPOS). Target audience: Thai restaurant owners locked into VDIT or other EPOS contracts. Pricing agreed by Korakot: Booking widget £29/month, Ordering widget £39/month, Bundle (both) £49/month, Full SiamEPOS Pro £89/month (widgets included). Core pitch angle: (1) ROI hook — a restaurant doing £3,000/month on Deliveroo pays £900 commission; our ordering widget at £39/month saves £861+/month even moving a third of orders. (2) Booking hook — replaces OpenTable (£299+/month) or FoodBooking.com (closing March 2027). (3) Switch trigger — they pay £49/month widgets + their EPOS fee (say £80-100). When EPOS contract expires: "Switch to full SiamEPOS at £89 — widgets included, no separate EPOS fee, you save £40-60/month." (4) VDIT angle — don't name them, but "already on another EPOS system? Start with just the widgets. Add the full system when you're ready." Deliverable: a sales pitch doc (Word or PDF) Korakot can use in demos and send to prospects. Also suggest what Nick should add to CLAUDE-Nick.md as ongoing competitive context. |
| 2026-05-17 | Claude | Maya | ✅ DONE — lite.html built and live. |
| 2026-05-17 | Pose | Krit | SEPOS-LITE-001 Phase 2b now unblocked — Lite backend deployed at lite-api.siamepos.co.uk (Railway, Online). DNS propagation pending for custom domain — Namecheap CNAME needed. Once DNS live, login works at siamepos-lite.netlify.app. Coordinate on endpoint read-scoping when ready. |
| 2026-05-17 | Korakot | Krit | **SEPOS-LITE-003 — Email login for Lite clients on app.siamepos.co.uk (Korakot directive 2026-05-17).** Lite clients are treated as full Pro clients but log in with email + password, not PIN. **What to build:** (1) Add a small **"Sign in with email →"** link at the bottom of the existing PIN login screen. (2) That link opens an email + password form — checks against the `lite_users` table (same table Pose built for the Lite onboarding). (3) On success: look up `restaurant_id` from `lite_users` → load `restaurants.plan` → launch the app with that restaurant's context and plan-based feature gating (Krit's SEPOS-LITE-002 gates already handle this). (4) JWT token stored in localStorage with 14-day expiry — same as Lite onboarding. Owner doesn't need to log in every day. (5) PIN screen stays completely unchanged for in-restaurant staff — this is purely an additional entry point, not a replacement. **One source of truth:** `restaurants.plan` controls what every user sees regardless of login method. Pro = full access. lite_* = feature gating. **No breaking changes** — existing Pro staff PIN login unaffected. Email login only activates when someone uses that path. Safe to ship alongside live Pro installs. |
| 2026-05-17 | Korakot | Krit | **SEPOS-LITE-002 — Plan-gated feature access (Korakot directive 2026-05-17).** Strategy change: Lite customers get the FULL SiamEPOS interface (ops.siamepos.co.uk or app.siamepos.co.uk), NOT a stripped-down dashboard. Features unlock/lock by plan. Onboarding at siamepos-lite.netlify.app stays — after signup they are redirected into the main app. **Feature gate matrix:** `lite_booking` (£29) → Reservations + table plan ✅, Customers + Campaigns ✅, Embed codes ✅, Kitchen ❌, Orders/takeaway ❌, Full EPOS (dine-in billing, staff, Z-report) ❌. `lite_ordering` (£39) → Orders/takeaway ✅, Kitchen screen ✅, Customers + Campaigns ✅, Embed codes ✅, Reservations ❌, Full EPOS ❌. `lite_bundle` (£49) → Reservations ✅, Orders ✅, Kitchen ✅, Customers ✅, Campaigns ✅, Embed codes ✅, Full EPOS ❌. `pro` (£89) → Everything ✅. **Implementation:** Extend Krit's existing plan gating in utils/plan.js — add `canAccessReservations(plan)`, `canAccessKitchen(plan)`, `canAccessOrders(plan)`, `canAccessFullEPOS(plan)` helpers. Gate nav tabs, admin sections, and routes accordingly. Show a friendly "Upgrade to unlock" message for locked features rather than a blank screen. Lite customers should feel they're using the real SiamEPOS — just with some tabs locked until they upgrade. |
| 2026-05-17 | Korakot | Krit | SEPOS-LITE-001 Phase 2a — GO. Build tenant-resolution middleware, tenantQuery helper, MULTI_TENANT flag, plan-based feature gating. Hosting decision: same Railway server as Pro (not separate). Dashboard: ops.siamepos.co.uk with EPOS tabs hidden for lite plans. Plan tiers: lite_booking £29, lite_ordering £39, lite_bundle £49, pro £89. Billing owner: SiamEPOS owns Stripe, restaurants pay SiamEPOS directly (subscription, not takeaway payments). Phase 2b (endpoint retrofit) — hold until first real Lite client is ready to onboard. Stripe sandbox keys are now in Railway env vars. Still need: create 4 Stripe Products and add price IDs to Railway (STRIPE_PRICE_LITE_BOOKING, STRIPE_PRICE_LITE_ORDERING, STRIPE_PRICE_LITE_BUNDLE, STRIPE_PRICE_PRO). JWT_SECRET also needs adding to Railway. lite-api.siamepos.co.uk DNS pending propagation — check Railway custom domain status before deploying Lite service. |
| 2026-05-17 | Claude | Krit | SEPOS-LITE-001 (new ticket): Build SiamEPOS Lite — widgets-only product for restaurants already on another EPOS. Needs: (1) Stripped-down onboarding: just restaurant name, address, phone, logo, menu upload, and basic settings (no table plan, no KDS, no staff setup). (2) Lite dashboard: show bookings and orders only — no EPOS tabs. Could be a simplified view of ops.siamepos.co.uk or a new lightweight page. (3) Account tier flag: a `plan` field on the restaurant record — 'lite_booking', 'lite_ordering', 'lite_bundle', 'pro'. Hide EPOS features for lite plans. (4) Stripe subscriptions (SEPOS-040) becomes prerequisite — lite plan needs billing before launch. (5) Widget embed codes: after setup, show the restaurant copy-paste embed codes for their existing website. Priority: design the DB schema + onboarding flow first, discuss with Korakot before building. |

---

## 📌 Upcoming Tickets (priority order)

| # | Ticket | Description | Assigned |
|---|--------|-------------|----------|
| 0 | SPA-004 | **Mobile Wallet loyalty card + voucher QR (Apple Wallet / Google Wallet).** Customer books → confirmation email has "Add to Wallet" button → loyalty card appears on phone with visit count, points, QR code. Every visit we push an update — points go up, stamps added, no app needed. Gift vouchers also delivered as wallet passes — staff scan QR at checkout, balance deducts, pass updates. Implementation: Apple (£79/year developer account + PassKit or Walletly service ~£50/month for certificate handling) + Google Passes API (free). Differentiator: no Thai spa in the UK is doing this. Makes loyalty feel premium. Build after SPA-002 + SPA-003. | Sam |
| 0 | INFRA-001 | Set up UptimeRobot monitoring for ALL services once SiamSpa domain is live. 5-min ping on: main EPOS API, Baan Siam API, main app, Baan Siam app, marketing site, demo site, Lite API, SiamSpa API, SiamSpa app. Alert to Korakot email + phone. Free plan covers 50 monitors. | Korakot |
| 1 | SEPOS-027 | Reservations + walk-in SMS (needs Twilio) | Krit |
| 4 | SEPOS-040 | Real Stripe payment on takeaway widget | Krit |
| 5 | SEPOS-021 | HMRC VAT submission (needs HMRC OAuth sandbox) | Krit |
| 6 | SPA-001 | Spa EPOS foundation (rooms, appointments, medical questionnaire) | Sam |
| 7 | SEPOS-ONBOARD-001 | AI-powered client setup wizard — 6-step onboarding (profile → menu import → table plan → staff → settings → allergen pre-fill). Target: 20–30 min setup vs 60–90 manual. Build after first 10 clients. | Krit |
| 8 | CONTENT-001 | **SiamEPOS tutorial video (Dojo RMS style).** Screen recording walkthrough of the full reservation setup flow: account + staff PINs → table plan → reservation settings → booking widget embed → managing bookings → going live. Me to write full script + narration. Sandy to build branded intro/outro + section title cards (navy/gold CI). Korakot to screen record with QuickTime (~15 min). Hand raw footage + script to Fiverr editor (£20–50) to assemble. Make after core product is stable. | Me + Sandy + Korakot |

---

## 🧠 How to Update This File

**At the start of your session:**
1. Read this file
2. Note what others have completed or are working on
3. Add yourself to "Active Work" if you're starting something

**At the end of your session:**
1. Move your active work row to "Recently Completed"
2. Add any handoff notes for other agents
3. Remove any outdated entries

**Format for a completed row:**
```
| 2026-05-14 | Krit | Short description of what was done | SEPOS-XXX |
```
